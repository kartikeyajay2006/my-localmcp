# Project Notes

## 2026-07-05

- **Real MCP stdio CI test isolated from xdist (issue #50).** `tests/test_distribution.py::test_repo_tools_respond_over_real_stdio` keeps its real server subprocess, three tool calls, and 5-second per-call timeout, but now carries a registered `serial` marker. The fast CI job runs the remaining non-slow suite under `-n auto`, then runs the serial subset without xdist. This addresses the observed Windows `anyio.BrokenResourceError` startup-starvation flake without hiding it behind a longer timeout or moving it onto the lifecycle critical path. A structural regression test locks together the marker registration, test decorator, and both workflow selections. Locally verified with 10/10 isolated stdio runs, both complete fast-suite partitions, and compileall.

## 2026-07-04 (6)

- **CI Phase 2 (offline wheelhouse) — measured win on the Windows critical path.** The `slow`/lifecycle job now prebuilds a wheelhouse (`pip download pip setuptools wheel mcp[cli] psutil`, ~4s with warm `cache: pip`) and runs the lifecycle tests with `PIP_NO_INDEX=1` + `PIP_FIND_LINKS=${{ runner.temp }}/wheelhouse`, so their ~6 in-test `venv`+`pip install-from-source` cycles resolve offline. Zero production change — `runtime.py`'s `subprocess.run` sets no `env=` and the lifecycle `_setup` helper spreads `**os.environ`, so the installer subprocesses inherit the vars. **Measured (PR #49 vs baseline run 28724512399): Windows `Native lifecycle` step 4m47s → 3m41s (~23% faster); Windows lifecycle job 5m29s → 4m29s (~1 min off the commit→merge critical path).** macOS lifecycle unchanged (~53s — macOS pip was never the bottleneck). Locally validated first: 41 wheels, no sdists, offline source-install + pip-self-upgrade succeed.

## 2026-07-04 (5)

- **CI-speed initiative, Phases 0-1 (spec: `docs/superpowers/specs/2026-07-04-ci-speed-design.md`).** Phase 0 (#14, merged): docs/meta-only PRs now skip the macOS+Windows test steps via an in-job `dorny/paths-filter` (job still runs so required checks report green — a workflow-level `paths-ignore` would leave them unreported and wedge branch protection); verified live green in ~12s with test steps skipped. Phase 0b (merged): scoped `push` to `main` + added a `concurrency` group so a PR runs the matrix once instead of twice (`push`+`pull_request`). Phase 1 (this change): split the single per-OS `verify` job into parallel `fast` (fast suite + compile) and `slow` (native lifecycle) jobs so per-OS wall-clock is `max(fast, slow)` not `fast + slow`, gated by a single `ci-gate` job (`needs: [fast, slow]`, `if: always()`, fails unless both aggregate results are `success`). Branch protection was repointed from the two per-OS `Python 3.12` contexts to the sole `ci-gate` context, so future job-layout changes no longer touch protection.

## 2026-07-04 (4)

- **Found live (dogfooding) that the `.claude` exclusion fix (#28) never reached existing installs, and fixed the root cause (#41).** A reinstall preserves `config.yaml` as durable data, and `load_config`'s `deep_merge` replaces lists wholesale, so a persisted `repo.exclude_dirs` written before `.claude` was added kept winning over the code default. Reproduced end-to-end on the real install: `refresh_index` indexed `.claude/POISON_TEST.md` + a fake `.claude/worktrees/` copy, and `prepare_context` ranked the worktree copy **READ FIRST #1 (score 89)** — exactly the poisoning #28 was meant to prevent. Fix (TDD): safety `exclude_dirs` are now **code-owned** — `load_config` rebuilds the effective list from the code default on every load and unions a new user surface `repo.extra_exclude_dirs` on top, so persisted config can only *add*, never override/shrink. Newly-added default excludes now reach every install regardless of config age. Also added `.pytest_cache` to the code-owned defaults (same class of tooling artifact). 3 new regression tests in `test_utils.py` (stale-persisted-override, `.pytest_cache` default, `extra_exclude_dirs` additive); the pre-existing `test_exact_configured_dir_names_are_still_excluded` only covered the fresh-config path. Rebuilt `neo-localmcp-v1.1.0.mcpb` (source changed). Full fast suite green (297 passed / 1 skipped), compileall clean. **Migration note:** users who hand-added excludes to `repo.exclude_dirs` must move them to `repo.extra_exclude_dirs`.

## 2026-07-04 (3)

- **Wizard now builds the Claude Desktop `.mcpb` into the repo.** Root-caused (systematic-debugging) an owner-reported "wizard isn't making the mcpb / doesn't say where" bug: the legacy `install.ps1` (lines 208-212) copied the versioned bundle to a fixed-name local copy at `~/.neo-localmcp/neo-localmcp.mcpb`, but the stdlib Python wizard that replaced the PowerShell installers never ported it. Owner reframed the fix (the `~/.neo-localmcp/` copy is "useless"): instead **build** the bundle into `packages/claude-desktop/`, versioned, **never overwriting** (a reinstall keeps old files and writes `-2`, `-3`, ...). New `neo_localmcp/mcpb_build.py` packs it with pure stdlib `zipfile` (a `.mcpb` is just a zip Claude Desktop reads via `manifest.json` -- no Node/`npx`, whose flakiness is already documented here). `RealBackend.run_operation` calls it after a successful install/reinstall (not uninstall), emits the path, and surfaces it in the outcome; a build failure degrades to a warning and never fails the install. Dev-only: `build_mcpb` returns `None` (silent skip) when the `mcpb/` staging inputs are absent, so it no-ops outside a source checkout. Deliberately scoped to the wizard, not the `setup.py install` CLI path. Design spec: `docs/superpowers/specs/2026-07-04-wizard-mcpb-build-design.md`.
- Builder also drops OS junk (`.DS_Store`) that the canonical `mcpb pack` excludes; restored the working-tree-deleted `packages/claude-desktop/mcpb/` staging inputs + committed bundles; rebuilt `neo-localmcp-v1.1.0.mcpb` in place so it embeds current source (was stale, missing `mcpb_build.py` -- would have broken `test_distribution.py` on CI). Verified: rebuilt bundle's file set == accepted `v1.0.10` bundle plus exactly the new-source modules (`mcpb_build.py`, `wizard/_ansi.py`); new `tests/test_mcpb_build.py` (7 tests: versioned name, no-overwrite counter, layout, junk/ignore exclusion, dev-only `None`, wizard surfaces path, wizard survives build failure). Full fast suite green (294 passed / 1 skipped), compileall clean.

## 2026-07-04 (2)

- Ran issue #10 (codebase quality audit) as a dedicated analysis-only pass -- **no code touched**, output is three docs + GitHub issues. Went depth-first module-by-module in the owner's priority order (tools -> repo_memory -> query -> cli -> server -> ollama_client -> client_setup/config/lifecycle -> wizard), reading every audited module in full with the `Read` tool (source is truth) and committing the report incrementally after each module. Deliverables: `docs/CODE_QUALITY_AUDIT.md` (module-by-module verdicts + `file:line` findings with severity), `docs/mcp-perf-log-issue-10.md` (every CLI command timed, incl. the never-before-exercised SLOW `qwen3-coder:30b` summarize path, ending in benchmarking recommendations for issue #9), and `docs/dogfooding-notes-issue-10.md`.
- **Headline quality finding:** the "right bones" hold up under a quality lens -- comment quality is a genuine project strength (matches CLAUDE.md's WHY-only standard, near-zero noise; `lifecycle.py` is the exemplar, verdict "clean, nothing actionable"). The debt is structural, not stylistic: `tools.py`'s `context_prepare` is a ~233-line 8-job function (high), and there's systematic DRY in the response renderers (tools.py, 3-4 copies), the Ollama result-dicts (ollama_client.py, ~10 copies), and the server error-wrappers (9 copies). One genuine docs-vs-code drift: `config.py`'s "config.yaml" is actually JSON.
- **Found one real new retrieval bug live while dogfooding:** `config.py`'s default `exclude_dirs` doesn't include `.claude`, so sibling `.claude/worktrees/agent-*` copies (this repo uses git worktrees for parallel agents) get fully indexed -- a `tools.py` query ranked 5 worktree duplicates above the real working-tree file (real file #6, score 152, vs duplicates at 505-568). Same class as the `.venv-phase14` pollution from 2026-07-03 (2), new directory. One-line fix; filed as an issue.
- **Ollama-as-junior-dev (owner refinement):** treated fast-rank/slow-summary output as a lead to chase then verify against source, never as authority. Datapoints: the fast reranker correctly smelled `context_prepare`'s SRP problem (corroborating my own finding) but hallucinated non-existent "audit logging" -- right on the smell, wrong on specifics. The fast `--ollama-rank` also *corrected* the worktree pollution by naming the canonical path #1. Perf: FAST path (qwen3:8b) is generation-bound (~14s eval, negligible warm load); SLOW path (qwen3-coder:30b) is load-bound (~14s one-time VRAM load, ~1.3s eval, 0.57s cache hit). Deterministic core stable (5 runs, 1 hash); Ollama fallback never blocked a response.
- Filed individual issues only for high/med-severity actionable findings (owner chose signal-rich); skipped already-filed #22/#23/#24/#26. Report PR opened into `main` as `meta/code-quality-audit-v2-wt`, `Refs #10` (not Closes -- owner may want follow-up passes). Not merged -- awaiting owner review.

## 2026-07-04

- Closed issue #8 (summary-cache behavior on an Ollama model swap) as a **documentation-only decision, no code change**: chose option 1 (status quo) after verifying in code that a `fast_model`/`summary_model` swap via `set-ollama`/config never errors or corrupts memory -- ranking is always live, `retrieval_boost` has no model column, and `set_ollama` only writes config. Cached summaries *are* model-tagged (`files.summary_model`, `section_summaries.model`) but are deliberately not invalidated on swap (`tools.py` `_summarize_section` only compares the cached model when `model` is passed explicitly; `summarize_file` defaults it to `None`, so the old-model summary is served until the source hash changes or the cache is cleared). Kept status quo rather than adding invalidate-on-swap machinery because summaries are advisory enrichment, never authoritative (ARCHITECTURE Safety model), so an old model's description of an *unchanged* file stays accurate -- and there's no concrete correctness problem to justify the extra machinery, matching the repo's minimalism convention. Documented the intended behavior in README.md (model-upgradability bullet, corrected to stop implying only ranking matters), CLAUDE.md Known gaps, and PROJECT_STATUS.md Known limitations, so a future contributor doesn't "fix" it unaware it's deliberate. Decision left open for owner confirmation in PR review, not treated as final. Dogfooding + a token-cost tally logged in `docs/dogfooding-notes-issue-8.md`.
- Closed issue #7: added `tests/test_schema_migration.py`, a migration-safety regression test that seeds a raw SQLite DB shaped like a pre-migration install (`files` missing the three `summary_*` columns, `task_queries` missing `retrieval_id`/`term_key`, `retrieval_boost`/`section_summaries` tables absent), runs current `repo_memory.init_db`/`index_repo` against it, and asserts no exception, pre-existing rows survive, and the additive columns/tables now exist -- pinning down the previously-empirical "additive migrations preserve data" guarantee. Verified the test is meaningful (a simulated init_db that forgot the ALTER/CREATE-IF-NOT-EXISTS steps leaves exactly those columns/tables missing, so the assertions would fail). Deliberately stayed in scope: no `PRAGMA user_version` and no migration runner were added -- the issue's suggested seed-old-DB-and-run-current-code approach was fully achievable without a schema-version marker, so adding one would have been scope creep. Test-only change, no `neo_localmcp/` source touched, so no `.mcpb` rebuild needed. Full fast suite green except the pre-existing environment-only `test_full_windows_lifecycle_via_setup` failure (documented 2026-07-03 (3): the subprocess `setup.py install` builds a bare venv where `psutil` isn't importable on this host; reproduced identically with the new test file removed, so not a regression); compileall clean.
- Dogfooded neo-localmcp's own `context`/`lookup` CLI to locate the target code (notes in `docs/dogfooding-notes-issue-7.md`, incl. a with-MCP vs. without-MCP token estimate). `lookup "init_db"` was exact and excellent; `context` correctly put `repo_memory.py` in the top 3 with the right line hints, but ranked `installer/migration.py` (filesystem-layout migration, the wrong sense of "migration") as #1 for a clearly SQL-schema query -- an overloaded-term mis-ranking filed as issue #24 (`fix`/`retrieval`).

## 2026-07-03 (14)

- Owner asked to tighten up the GitHub-native side of the project (branch protection, PR/issue conventions, code review, Actions, repo metadata), being new to everything beyond branches/commits/PRs. Made the repo public (secrets-scanned history first, clean) since GitHub's branch protection and rulesets are both paid-only for private repos on the free plan. Set branch protection on `master`: PR required, both CI checks (macOS/Windows) must pass, branch must be up to date, no force-push/deletion, enforced even for the admin.
- Found and fixed a real CI break during this work: every run since the wizard PRs had been failing (`test_distribution.py` -- the versioned `.mcpb` bundle was never rebuilt after adding `neo_localmcp/wizard/`), and it had been merged to `master` twice without checking CI status first. Rebuilt the bundle, verified the full suite locally (was only running `compileall` all session, not full `pytest` -- a real process gap), fixed, merged properly this time with CI confirmed green first.
- Added LICENSE (was declared in `pyproject.toml` but no actual file existed), set repo description/topics, enabled Dependabot security updates + secret scanning + push protection, added a README "north star" section reflecting a messaging pivot -- cross-checked against the actual code (audited hash-aware indexing, incremental refresh, bounded excerpts, Ollama ranking, `git apply --check` -- all genuinely implemented as claimed) rather than repeated as marketing copy.
- Designed a `type(area): description` issue/PR taxonomy grounded in Conventional Commits + the Angular extension, with two deliberate documented deviations: `meta` (project-as-repo, not in any standard convention) and `chore` kept at its narrow standard meaning (non-src/test only; code cleanup is `refactor` instead). Areas mirror the real module map (wizard > installer + client-integration; mcp-tools > mcp-mgmt + mcp-toolkit; ollama; retrieval; benchmark). Documented in `.github/CONTRIBUTING.md`.
- Measured (not assumed) the CI speed complaint: pulled real per-step timing showing macOS and Windows already run in parallel as matrix jobs; Windows "Fast tests" + "Native lifecycle" dominate wall-clock, and the fast-suite timing matched local Windows execution almost exactly, meaning it's the suite's genuine I/O/subprocess-heavy runtime, not GitHub-infra overhead. Added `pytest-xdist` (`-n auto`) to the fast suite only (native-lifecycle tests stay serial -- they build real venvs/process trees and risk cross-worker collisions). First measured ~5x locally, but that was measured on a 16-core machine against GitHub's 4-vCPU Windows runner -- real CI gain was ~1.4x (149s -> 108s). Corrected the PR title/body and amended the commit message once this was known, rather than let the inflated number stand. Filed the two real remaining levers (split CI by area; skip test execution on meta/docs-only changes) as Issues, not PRs, since both need groundwork (test reorganization; a path-filter that doesn't break required-status-check semantics) that doesn't exist yet.
- Switched merge strategy from squash back to a real merge commit, at owner's explicit preference, after discussing the tradeoff: squash/rebase (both, since GitHub performs rebase server-side and also produces new commit SHAs) always leave local branches showing as "not merged" to `git branch -d`, requiring `-D`. A real merge commit preserves ancestry, avoiding that entirely, at the cost of a noisier `git log` (mitigated with `git log --first-parent master` for a flattened view). This required disabling branch protection's `required_linear_history` first -- merge commits are non-linear by nature, and GitHub validates that squash/rebase can't both be disabled while linear history is required.
- Created the actual `type:`/`area:` GitHub labels (18 total) and retitled/labeled issues #7-#14 to match the finalized taxonomy -- two of them (#13, #14) had been filed with titles that predated the final type list (`ci:` isn't one of the 9 agreed types) and needed correcting.
- This entry itself, plus a new `CLAUDE.md` "GitHub workflow" section, are catch-up documentation for a real gap: none of the above was written down anywhere a future agent session reads first, so it would otherwise have been rediscovered by hitting the same walls (failed direct push, failed squash-merge attempt) instead of just reading it.

## 2026-07-03 (13)

- Owner asked to verify the wizard's real (non-`--fake`) backend is genuinely wired to the actual install/reinstall/uninstall lifecycle, "maintain carefulness" -- i.e. don't just trust the code, prove it against the real installer without touching this machine's real `~/.neo-localmcp` (which may back live Claude Desktop/Code sessions per `PROJECT_STATUS.md`'s prior stress test) or the real Claude/Codex client configs.
- Followed the exact isolation pattern `tests/installer/test_windows_lifecycle.py` already uses: `NEO_LOCALMCP_HOME` pointed at a disposable scratch directory, invoking `setup_wizard.py` as a real subprocess with piped numbered input, selecting zero clients (so no real `claude mcp add`/config-file writes could occur regardless of HOME). First attempt also overrode `HOME`/`USERPROFILE`/`APPDATA` for extra isolation like the test file does, but that broke the *launching* interpreter's own `psutil` import (pip had installed it to user-site, which Windows resolves via `APPDATA`) -- reverted to leaving those real, since zero-client selection already makes them unnecessary for this verification. Worth remembering for future subprocess-isolation attempts on this host.
- Verified for real, fully isolated: install (real `python -m venv` + `pip install` into a candidate runtime, real MCP handshake, real `doctor` check, `doctor` on the resulting install returned `ok: true`) -> reinstall -> uninstall (runtime-only: venv removed, `config`/`sqlite` durable data survived, `detect()` correctly reported `data-only`) -> install-from-data-only (correctly reused preserved data) -> uninstall full-wipe (typed-phrase gate, `assume_yes=True` correctly passed through to skip the installer's own redundant confirm prompt). Confirmed throughout that every touched path stayed under the scratch root and the real `~/.neo-localmcp` was never touched.
- **Found and fixed a real bug this surfaced, invisible under `--fake`**: `console.py`'s `_save_prefs()` ran unconditionally after *any* successful operation, including uninstall. For a full wipe this meant `RealBackend.save_prefs()`'s `path.parent.mkdir(parents=True, exist_ok=True)` silently recreated the just-deleted `config/` directory and wrote `wizard-prefs.json` back into it immediately after `delete_managed_root`'s `shutil.rmtree` -- undermining the "wipe everything" guarantee. `FakeBackend.save_prefs()` is pure in-memory with no filesystem write, so this never showed up in any of the extensive `--fake` testing from the prior session. Fixed by skipping `_save_prefs` entirely for `OP_UNINSTALL`. Re-verified: a fresh install-then-full-wipe cycle now leaves the scratch root completely absent, zero leftover files.

## 2026-07-03 (12)

- Owner confirmed the wizard works perfectly on macOS, then asked for 5 quick improvements: (1) Y/N/B back support at every step, (2) the running summary should clearly show which clients were selected + their path + subtext, (3) the Ollama model picker should run `ollama list`-equivalent, sorted alphabetically, with sizes, (4) every default should show as `[Default: <val>]`, (5) the fast/ranking model picker should explain what "fast model"/"ranking" means with a quick example.
- Implemented back-navigation as a small phase machine in `console.py`: `_GoBack`/`_Skip`/`_Abort` exceptions and a `_run_phases()` driver where each phase either raises `_Skip` (nothing to ask given current answers -- passed over transparently in whichever direction the driver is already moving) or prompts and lets `_GoBack` propagate on "b"/"back". This correctly re-evaluates conditional phases live on every visit (e.g. turning Ollama config off skips its dependent phases in both directions), verified with multi-level back tests (backing out of fast-model-pick returns to base-url; backing out of summary-pick returns to fast-pick; wrong wipe-confirmation phrase retries locally but "back" exits to mode selection). All 4 input primitives (`_ask_int/_multi/_text/_yesno`) now accept "b"/"back" and show `[Default: ...]`. A real bug surfaced during this work: since phases can now be revisited, the old `self.summary.append(...)` pattern would duplicate a summary line each time its phase re-ran; fixed with an idempotent `_set_summary()` (replace-by-label) used everywhere.
- Client selection detail (path + what-it-does + `[manual step]` tag) now persists in the running "Your choices so far" summary on every subsequent screen once chosen, not just at confirm -- verified live.
- `OllamaInfo` gained `model_sizes: dict[str,str]`; `real_backend.ollama_info()` fetches per-model sizes via a direct `/api/tags` call (`ollama_client._request_json`, best-effort, never fatal) and both real+fake backends now return `installed_models` alphabetically sorted. `human_size()` added to `backend.py` as a shared formatter.
- Fast-model phase explains ranking with a concrete example ("for a task like 'fix the login bug', it decides which files are probably relevant"); summary-model phase explains file-summary generation.
- Verified end-to-end on Windows via piped-input runs: full install completion, declined-proceed abort, multi-level back navigation, uninstall wrong-phrase-then-back-to-mode, manage-clients, reinstall, and standalone Ollama config -- all correct. Not yet re-tested on macOS after this change.

## 2026-07-03 (11)

- Owner wants to test the wizard on macOS and asked that paths auto-update per OS (Mac must show Mac-style paths). Real backend was already correct — it computes everything from the live OS (`ManagedPaths.from_environment()`, `client_setup.client_status()` via `platform.system()`/`Path.home()`, and `_os_label` returns "macOS" for posix+darwin), so on a real Mac it yields `/Users/name/...` forward-slash paths and the `.mcpb` under `~/.neo-localmcp`. Confirmed `_os_label` returns "macOS" for a simulated posix/darwin env.
- Fake backend was the gap: it split only Windows-vs-not and showed `~/...` for everything non-Windows. Rewrote its path helpers to key on `sys.platform` (`_fake_os()` -> windows/macos/linux) and build paths with the correct home + separator per OS: Mac shows `/Users/you/...` (forward slashes), Windows `C:\Users\you\...` (backslashes), Linux `/home/you/...`. Verified all three by monkeypatching `sys.platform` on this Windows host. Removed now-unused `platform` import.

## 2026-07-03 (10)

- Owner approved the direction ("exactly what I'm looking for") and asked to lock it in and polish the fake guided installer. Applied the clients-step treatment (a brief "what this step is about" explainer + labeled subtext) consistently to every step: added a `_explain()` helper in `console.py` and used it on the main menu, clients, Ollama step, both model pickers (fast = "smaller/fast is best", summary = "larger code model is best"), uninstall, and confirm. `_pick_model` gained a `hint` param so fast vs summary each explain their purpose. Walked the full fake install flow end-to-end on Windows to confirm every screen reads cleanly and the running summary builds (Operation -> Clients -> Ollama) by the confirm screen.

## 2026-07-03 (9)

- Owner spotted that the wizard's client-selection paths were wrong/misleading and asked to verify against `client_setup.py`. Confirmed: Claude Desktop is NOT configured by writing `claude_desktop_config.json` (the code explicitly says "Direct claude_desktop_config.json editing is intentionally no longer performed") — it's a `.mcpb` package at `~/.neo-localmcp/neo-localmcp.mcpb` that the user installs manually via Settings > Extensions > Advanced settings (`setup_claude_desktop` returns `applied: False, manual_install_required: True`). Claude Code isn't a single config file either: slash commands go to `~/.claude/commands/neo-localmcp` and the MCP server is registered via a `claude mcp add --scope user` CLI call.
- Fixed both backends to tell the truth: `ClientOption` gained `detail` (what the path is) and `manual` (can't be automated) fields. Real backend now shows the `.mcpb` path for Claude Desktop (from `paths.root / "neo-localmcp.mcpb"`), labels Claude Code as slash-commands-dir + CLI registration, and labels Codex as a config.toml block; `apply_client_changes` surfaces Claude Desktop's manual `.mcpb` install instructions instead of silently reporting success. Fake backend mirrors the same paths/details. Console clients screen now leads with a brief explainer of the step and prints each option as label + `path:` + what-it-is, with Claude Desktop tagged `[manual step]`. Verified real `client_options()` resolves the correct live paths on this Windows host; fake render confirmed.

## 2026-07-03 (8)

- Owner found the Textual wizard over-engineered ("we went balls to the wall") and asked to simplify the UI to a plain full-screen numbered wizard: numbers to select options, a summary built up as you answer. Committed the Textual version first (`0c31fcb`) as a revert point, then replaced the UI.
- Replaced `wizard/app.py` + `wizard/screens/` with a single stdlib `wizard/console.py` (clear-screen + running-summary + numbered `input()` loop). Dropped `textual` entirely: `[wizard]` extra now pins only `psutil` (already a base dep), `preflight.py`'s `WIZARD_DEPENDENCIES` is `("psutil",)`, and `setup_wizard.py` imports `run` from `console`. The `WizardBackend` seam paid off exactly as designed — `backend.py`/`fake_backend.py`/`real_backend.py` were reused with zero changes; only the UI layer swapped.
- Replaced all non-ASCII glyphs (em-dash, middle-dot, ellipsis) with ASCII across the wizard, because piped/legacy Windows consoles rendered them as replacement chars; verified output is ASCII-only.
- Verified on Windows: `compileall` clean; drove the fake install flow and the fake config-ollama flow end-to-end via piped numbered stdin — running summary builds correctly, live steps stream, menu adapts to installed state afterward, quit works. Still not run interactively by a human or on macOS.

## 2026-07-03 (7)

- Built a guided terminal installer (`setup_wizard.py` + `neo_localmcp/wizard/`) on branch `feature/setup-wizard-tui`, per owner request to make install/uninstall/reinstall/config simple for end users. Textual-based TUI with arrow-key navigation and a dim-grey explainer line under every option. Brainstormed the design first (spec at `docs/superpowers/specs/2026-07-03-setup-wizard-tui-design.md`), then owner said to build it all the way through (fake + real backends), skipping formal tests.
- Architecture keeps a clean seam: screens depend only on a `WizardBackend` Protocol (`wizard/backend.py`), never on `installer/` directly. `fake_backend.py` (in-memory, side-effect-free, `--fake`) and `real_backend.py` (wraps the existing `installer` operations + `config`/`ollama_client`/`client_setup`) are interchangeable. The real backend adds no lifecycle policy — it builds an `OperationContext` exactly like `setup_cli.build_context` but with a `Reporter` whose output is forwarded live into the TUI log.
- Menu is adaptive to detected state: first-time clone → Install / Configure Ollama / Quit; returning user (healthy) → Reinstall-or-Repair / Configure Ollama / Manage clients / Uninstall / Quit. Ollama model selection is populated from the live `ollama list` (`ollama_client.status().installed_models`) as radio choices, falling back to text inputs when Ollama is unreachable. Standalone Ollama config writes `config.yaml` only (no rebuild). `wizard-prefs.json` remembers last client/model choices (UX only, non-authoritative). Only one destructive path in the UI: full wipe under Uninstall, behind the same typed `DELETE ALL NEO-LOCALMCP DATA` gate the CLI uses. Clean-install was intentionally left out of the menu.
- Dependency bootstrap: `[wizard]` extra added to `pyproject.toml` (`textual>=0.60,<7`, `psutil`). `setup_wizard.py` + `wizard/preflight.py` are stdlib-only above the gate — on a bare clone the wizard detects missing deps, offers to `pip install -e ".[wizard]"`, and re-execs. Solves the same host-`psutil` gap noted in (6), for the wizard path.
- Verified live on this Windows host (Python 3.14.5, textual 6.12.0 installed for testing): `python -m compileall` clean; a headless Textual `Pilot` smoke script drove the fake flow end-to-end (12 checks: home menu builds for absent+healthy states, every screen composes, the threaded worker runs an operation and renders a result panel). Real backend `detect()` correctly reported this machine as `data-only` with claude-code+codex registered and Ollama unreachable (not running); both Ollama-screen branches (radio list with models, text-input fallback without) compose. Not yet run interactively by a human, and not yet exercised on macOS.

## 2026-07-03 (6)

- Owner asked whether a fresh clone actually installs cleanly (does `setup.py` need a `.venv`? how do you get its own required packages on Windows/Mac/Linux? does the installed MCP need its own venv?). Investigated by reading `setup.py`/`setup_cli.py`/`installer/*` rather than guessing, then reproduced live: built a genuinely bare venv (only `pip`, nothing else) and ran `setup.py install --dry-run` against it -- crashed with `ModuleNotFoundError: No module named 'psutil'` from `installer/processes.py`'s top-level import, reached via `setup_cli.py`'s unconditional `from neo_localmcp.installer import (...)`. `mcp` itself is not needed by the host (only imported inside `server.py`, validated later via a subprocess using the candidate venv's own Python), so `psutil` alone is the missing piece.
- Confirmed CI (`setup-v2.yml`) never hits this because it runs `pip install -e ".[test]"` before any lifecycle test, which happens to install the full `dependencies` list (`mcp`, `psutil`) first -- masking the gap for as long as nobody actually tries the README's own instructions from a truly bare interpreter.
- Fixed: added a `python3 -m pip install "psutil>=5.9,<8"` prerequisite line to `README.md`'s Install section, ahead of the `setup.py install` commands. Re-verified with the same bare-venv reproduction (installed `psutil` first, dry-run then completed cleanly through the full ordered action plan). Chose documentation over restructuring the import (e.g. deferring `psutil` import inside `processes.py`) since the host needs `psutil` almost immediately anyway.
- Confirmed the other two questions are non-issues by design, not gaps: `setup.py` is meant to run from a bare system interpreter (never needs its own venv to *run*), and it already builds/validates/promotes a fully isolated managed venv at `~/.neo-localmcp/venv` for the installed `neo-localmcp`/`neo-localmcp-server` executables (`installer/runtime.py`).

## 2026-07-03 (5)

- Owner asked to merge everything back to master as v1.0.10. Before merging, checked PR #2's CI status and found it genuinely red (not flaky): `macos-latest / Python 3.12` failed with a `StopIteration` in the new `test_excerpt_centers_on_highest_weight_hint_not_first_by_line` test (from the #2 fix), while the same test passed locally and on `windows-latest`.
- Root cause was a real, previously-hidden bug, not a test artifact: `utils.rg_search`'s pure-Python fallback path (used when `rg` isn't on PATH) did `needle = query.lower(); if needle in line.lower()` -- a literal substring match. But the only caller (`tools.py`'s batched term search) always passes a regex-escaped alternation like `"(?:worker|RareMarkerNeedle)"`, which never appears verbatim in real source text. So on any host without ripgrep, the entire batched-term search silently returned zero rows -- for one term or many. This had been true all along; it just never surfaced because every dev machine used so far (and `windows-latest` CI) happens to have `rg` on PATH, while GitHub's `macos-latest` runner does not, and no prior test exercised a candidate that depended purely on batched-search matching (existing tests' targets were also discoverable via real symbol names, which route through a different, unaffected code path).
- Fixed: the fallback now compiles the query as an actual `re.IGNORECASE` regex and searches with it. Verified with a monkeypatched `which()` returning `None` to force the fallback path deterministically (mirrors the CI condition exactly, without needing to actually uninstall ripgrep locally) -- `tests/test_utils.py::test_rg_search_fallback_interprets_the_query_as_a_regex`. Rebuilt the `.mcpb` bundle again (same `utils.py` edit staled the distribution byte-identity test); full fast suite green (278 passed / 3 skipped / 6 deselected), `compileall` clean.
- Lesson for future sessions on this host: this machine has `rg` on PATH via two separate install locations (`~/.local/bin` and WinGet Links), so any test whose correctness genuinely depends on ripgrep vs. its fallback needs either a `which()` monkeypatch (fast, reliable) or an explicit PATH-stripped subprocess run (slow -- the fallback's full-repo line-by-line scan is dramatically slower than `rg` and can time out on anything but a trivial tmp repo) to actually exercise the fallback locally.

## 2026-07-03 (4)

- Fixed the multi-hint excerpt-centering/retrieval-boost-overlap gap found during the stress test (2026-07-03 (2) below). TDD: added `tests/test_context.py::test_excerpt_centers_on_highest_weight_hint_not_first_by_line` and `tests/test_retrieval_memory.py::test_follow_up_pull_on_a_secondary_hint_still_counts_as_followed`, both reproduced red first (first attempt at the centering test accidentally used a term that was itself an exact symbol name, which an earlier code path already handled correctly and bypassed the bug entirely -- had to redesign it so the winning term is plain text inside another function, matching the real `num_predict`-inside-`chat()` repro precisely, before it actually exercised the fallback path).
- Root cause was one level deeper than first documented: `tools.py`'s fallback excerpt-centering (`hint_numbers[0]`) picked whichever hint sorted first by ascending line number, with no regard for which reason/term produced it. Fix: `_add_candidate` now maintains `line_hint_weights` (line number -> max evidence weight seen for that line), and centering picks `min(weights, key=lambda line: (-weights[line], line))` -- highest weight wins, ties break to the earliest line. This dict is popped off each candidate before the response is serialized so it doesn't leak into the public `candidate_files`/`read_first` JSON. First debug attempt showed even a "clean" test scenario had a confound: a term that was both a filename substring AND a real symbol name accrued weight from *two different reason tiers* (symbol-name-match reasons score 16/10, plain rg_search-text-match reasons score 8/4) that aren't really comparable in kind -- redesigned the test file so the competing terms only ever produced same-tier rg_search hints, which is what made the weight comparison meaningful.
- Second part: `sections_for_memory` fallback entries now carry `hint_lines` (every other hint line for that path not already covered by the shown window), and `repo_memory.record_retrieval_feedback` credits a pull as "followed" if it overlaps the shown window *or* any of those other hint lines, not just the one arbitrarily-centered window.
- Rebuilt the versioned `.mcpb` bundle again (same reason as the previous fix -- editing `neo_localmcp/tools.py`/`repo_memory.py` staled `test_built_mcpb_embeds_current_package_bytes`); full fast suite green (277 passed / 3 skipped / 6 deselected slow), `compileall` clean.

## 2026-07-03 (3)

- Fixed the `exclude_dirs` exact-match gap found during the stress test below. TDD: added `tests/test_utils.py` (3 tests) proving the venv-pattern-exclusion test fails before the fix and passes after, while a control test confirms a real directory that merely contains "venv" as a substring (`eventvenue/`) is still indexed -- the fix is a glob prefix match via `fnmatch`, not a substring match. Changed `neo_localmcp/utils.py::scan_repo_files` to match `exclude_dirs` entries as patterns (`_is_excluded_dir`) instead of exact-set membership, and broadened `config.py`'s defaults `.venv`/`venv` to `.venv*`/`venv*` (a plain pattern with no wildcard still matches only that literal name, so this is fully backward compatible).
- Full suite run surfaced two failures, one caused by this change and one pre-existing: `tests/test_distribution.py::test_built_mcpb_embeds_current_package_bytes` failed because the versioned `.mcpb` bundle wasn't rebuilt after the `config.py` edit -- rebuilt it manually (the repo's `npx --yes @anthropic-ai/mcpb` invocation doesn't resolve the `mcpb` binary correctly under this host's Git Bash/npx combination; worked around by invoking `node <npx-cache>/@anthropic-ai/mcpb/dist/cli/cli.js pack . <target>` directly against the same staged directory the script builds) and reran to confirm all 11 distribution tests pass. `tests/installer/test_windows_lifecycle.py::test_full_windows_lifecycle_via_setup` failed both with and without this change (verified via `git stash`) -- it builds a real candidate venv via `setup.py install` and something in that nested subprocess's own `pip install` doesn't end up with `psutil` importable; not a regression, not chased further since this test's durable gate is CI/real VMs per `docs/1.0.10_PLAN_PENDING.md`, not this interactive host. Full fast suite (`-m "not slow"`) is 276 passed, 3 skipped; `compileall` clean.
- This host's bare system Python (`C:\Program Files\Python314\python.exe`) had no project dependencies installed at all (`psutil` missing) when this session started; ran `pip install -e ".[test]"` against it to make `pytest` runnable locally. Worth remembering for future sessions on this machine -- there's no repo-local `.venv` for this checkout, unlike the managed `~/.neo-localmcp` runtime.

## 2026-07-03 (2)

- Fast-forwarded `codex/settings-rework` (0 commits diverged, a strict ancestor of `claude/settings-rework-phase8beyond`) to the claude branch's HEAD and opened [PR #2](https://github.com/NeelAPatel/neo-localmcp/pull/2) into `master` per owner instruction ("claude branch -> codex branch -> master"). No rebase/force-push needed since it was a pure fast-forward.
- Live 12-tool MCP stress test against the real Windows `~/.neo-localmcp` install (owner explicitly chose the real install over an isolated temp home, wanting realistic evidence). Full findings in `PROJECT_STATUS.md`'s Verified/Known limitations sections; summary: started from 0 indexed files for this repo, confirmed deterministic Ollama fallback, watched retrieval-boost cross its shown>=3 threshold live, and measured bounded-context token cost at roughly 8-10x cheaper than raw whole-file reads for an equivalent query.
- **Found and fixed a real, live ranking-pollution bug during the test, not a hypothetical**: a 96MB leftover `.venv-phase14` disposable test venv (untracked, git-ignored, dated back to the completed Task 14 Windows work) was sitting in this repo's own working tree. `neo_localmcp/config.py`'s default `exclude_dirs` list does exact-name matching (`.venv`, `venv`) and doesn't match differently-named venvs, so the indexer picked it up wholesale. A query for `ollama_client` internals returned 100% vendored `_pytest` source and zero real files as a direct result -- the top-ranked "why" hits were literally pytest's own `__init__.py`/`legacypath.py`/`pytester.py`. Deleted the directory (after an interrupted first attempt -- the harness's own auto-mode classifier correctly blocked a `repo_status` call following an unauthorized `rm -rf` on a pre-existing, not-user-named directory; owner then explicitly authorized it) and ran `refresh_index`, which cleanly pruned all 60 stale rows (`removed: 60`) and restored correct ranking (`ollama_client.py` score 443, essentially all other candidates real source/tests). `exclude_dirs` doing exact-match rather than prefix/glob matching is a real gap worth fixing -- it doesn't even cover this project's own installer's `.venv-nlm-v<version>` naming convention. Not fixed in code this session, only worked around by deleting the offending directory.
- **Found a second real gap, not yet fixed**: `repo_memory.record_retrieval_feedback`'s overlap check only compares a `file_excerpts` pull against the single primary excerpt window persisted for that path (the first `CURRENT SOURCE EXCERPTS` block), not the other "around line N" hint locations listed in the same `READ FIRST` entry's `why` reasons. Reproduced directly: pulling `neo_localmcp/ollama_client.py:260-320` -- which contains lines 280 and 308, both explicitly listed as hint locations and line 308 being the literal `def chat(..., num_predict...)` match for the query term -- was scored `overlap: false` ("corrected"), while pulling the primary-shown 8-47 window scored `overlap: true` even though it doesn't contain the query-relevant `num_predict` code at all. The implicit-feedback signal can therefore misjudge a legitimately-followed hint as a correction.
- Also noted: the MCP transport itself has a response-size safety cap independent of the requested `token_budget` -- two `token_budget=1200` calls were cut off mid-response (`"[truncated by MCP safety cap]"`) before neo-localmcp's own budget-accounting footer could render, while `token_budget=400`/`600` calls completed cleanly and self-reported `estimated_tokens` within ~1.5% of the request. Worth knowing when choosing budgets for larger multi-candidate queries.

## 2026-07-02

- Task 16 release closeout: native Python 3.12 CI passed on macOS and Windows (Actions 28632598884); renamed the sole lifecycle entrypoint to `setup.py`, exposed repeatable fresh-install `--client` selection, archived legacy `.ps1`/`.sh`/`.cmd` installers and their historical integration test under `_LegacyInstallers/`, synchronized version 1.0.10, and updated active documentation. After an owner-requested complete wipe (including 21 retrieval-memory rows and manual Claude Desktop extension removal), a clean real install passed runtime promotion, MCP handshake, doctor, canonical-layout checks, and Codex/Claude Code registration. Linux remains explicitly deferred.
- Owner narrowed the 1.0.10 release gate to macOS and Windows; Ubuntu/Linux support is deferred. The Actions matrix no longer runs Ubuntu, and no Linux parity claim will be made in release documentation.
- Phase 15 implementation: added a Linux-only real setup-v2 lifecycle test reusing the proven POSIX acceptance helpers, plus a Python 3.12 GitHub Actions matrix for Ubuntu/macOS/Windows that runs fast tests, the native slow lifecycle, and compileall without downloading Ollama models. Windows verified Linux collection/skip behavior and compileall only; the actual Linux/macOS jobs require a pushed clean checkout, so the three-OS gate remains pending and Phase 16 is not authorized yet.
- Phase 14 Windows x64: proved the staging-promotion defect first, then fixed it in shared Python by rewriting the exact embedded venv prefix in Windows distlib launchers; added the missing `neo-localmcp-server` console entry point. The real isolated lifecycle passed live MCP reinstall/uninstall, preserved config/SQLite/client data and reuse, clean/full wipe, cancellation safety, broken/interrupted recovery, and unrelated Python survival; a registered parent+descendant tree that ignored graceful shutdown was terminated without touching its unrelated sibling. Systematic Windows fixes also corrected TOML-escaped Codex launcher verification, canonical registry/config assertions, platform guards, and an acceptance-test SQLite handle that macOS deletion had masked. Final verification: 281 passed, 4 skipped in 670.96s; Windows slow lifecycle 2 passed in 221.74s; compileall clean. Environment deferrals: no Python 3.12 installation (3.14.5 used) and no live Claude Desktop/uv.exe tree. Legacy installers remain in place.

- Phase 13 whole-branch review found that setup-v2 orchestration discarded client-removal results and unconditionally reported `removed-client-registrations`; a failed `claude mcp remove` could therefore be followed by runtime/root deletion while leaving a stale client target. Added explicit removal `ok`/`error` results and lifecycle validation: failed automated cleanup now aborts default uninstall, full wipe, and clean install before destructive removal, while Claude Desktop's manual-only cleanup is surfaced as a warning. Regression coverage exercises all three destructive paths and the real client helpers.
- Re-verified Phase 13 on macOS: 268 fast tests passed (6 platform skips), and the three unrestricted real tests passed for candidate promotion, installed-endpoint verification, and the complete isolated lifecycle. Windows x64/Claude Desktop behavior remains Phase 14, not inferred from the Mac result.

## 2026-07-01 (9)

- Found and fixed a Windows-only `apply_patch`/`apply-patch` bug during a full command-inventory test against an external repo (AntiNotepad): `apply_unified_patch` in `neo_localmcp/tools.py` wrote the incoming patch to a temp file via `tempfile.NamedTemporaryFile("w", ..., encoding="utf-8")` with no `newline=""`, so Python's text-mode write silently converted every `\n` to `\r\n` on Windows. Against any LF-terminated target file (the common case), the CRLF temp patch no longer matched file context and `git apply --check` rejected an objectively valid diff -- reproduced directly (CRLF temp -> rc 1, same patch written with `newline=""` -> rc 0). One-line fix: added `newline=""` to the `NamedTemporaryFile` call. Verified via an isolated scratch git repo (not a real project) exercising the real `neo-localmcp apply-patch` CLI end-to-end: check-only now passes, real apply succeeds, `git diff --stat` shows a clean single-line append. Rebuilt `packages/claude-desktop/neo-localmcp-v1.0.9.mcpb` to bundle the fix; full suite still green (95 passed). No version bump -- same 1.0.9 train, not yet tagged/released.
- Noted separately (not a bug, not touched): applying a patch to an LF file still leaves the file CRLF afterward on this machine, because global `core.autocrlf=true` normalizes line endings on any `git apply`/checkout -- independent of neo-localmcp, same as plain `git apply` would do.

## 2026-07-01 (8)

- Cut and installed 1.0.9. Bumped `__version__` to 1.0.9 in lockstep across `neo_localmcp/__init__.py`, `pyproject.toml`, and `packages/claude-desktop/mcpb/manifest.json` (the three that must always match). Rebuilt the versioned bundle in-repo as `packages/claude-desktop/neo-localmcp-v1.0.9.mcpb` (removed the stale v1.0.8 one). Upgraded the real `~/.neo-localmcp` CLI live via `install.ps1` (no `NEO_LOCALMCP_HOME` override): the prior state had only `config.yaml` + `servers/` (no venv), so this built a fresh single `.venv-nlm-v1.0.9`, bin launchers, copied the v1.0.9 `.mcpb` to the fixed-name local path, and created `repo-context.sqlite` via init. Verified: venv package reports 1.0.9, CLI usage now lists the new `remove-client` subcommand (9f), `doctor` -> `ok: true` (config/db/Ollama healthy, both configured models found by the 9b check), distribution tests green at the bumped version. The separately-installed Claude Desktop extension was left untouched -- swap in `neo-localmcp-v1.0.9.mcpb` manually via Settings > Extensions when ready.

## 2026-07-01 (7)

- 1.0.9 phase 9g (`docs/1.0.9_PLAN.md`, final Opus/High phase): P5 retrieval-boost audit + upgrade-persistence guarantee + config-tuning. **Audited the wiring live before assuming** (per the plan): a real multi-call session against this repo (isolated `NEO_LOCALMCP_HOME`) ran `prepare_context`+`file_excerpts` five rounds and watched the `retrieval_boost` table -- confirmed `shown` accrues per returned section, `followed` only on the section actually pulled back, a boost appears exactly once `shown >= min_shown` (round 3: +3, round 4: +4), grows with net-followed, and that shown-but-not-followed paths stay `followed=0` (silence not penalized). Also confirmed non-markdown files participate via the `(path, "")` boost key. The mechanism works and is *conservative*, not noisy.
- **Upgrade-persistence: found the "byte-identical" framing in the plan was subtly wrong, corrected to a data-level guarantee.** Wrote a test that seeds `retrieval_boost` rows, stages a stale different-version venv, runs a real `install.ps1` upgrade sweep, and checks the rows survive. First cut asserted the db file was byte-identical -- it FAILED, and a probe (systematic-debugging, not a guess) showed why: `install.ps1` runs `neo-localmcp init`/`doctor`, which legitimately open and write to the db (schema init, indexer version in `metadata`, FTS pages, the SQLite header change counter), so the file bytes DO change on every upgrade even for a full-schema db -- verified live. That is correct behavior, not corruption; the memory *data* is what must survive, and does. Rewrote the assertion to exact row-tuple identity (seeded via the real `repo_memory.init_db` so the schema can't drift). This is the never-before-tested claim from CLAUDE.md's known-gaps now closed.
- **Tuning: no constants changed, but they're no longer hard-coded.** The audit found no evidence to move any value (it showed the signal is clean, the opposite of the "raise MIN_SHOWN if noisy" case the plan anticipated). Promoted `RETRIEVAL_BOOST_CAP`/`RETRIEVAL_BOOST_MIN_SHOWN` from module constants to config (`memory.retrieval_boost_cap`/`retrieval_boost_min_shown`, defaults 8/3 = the unchanged constants, which remain the fallbacks), matching how `retrieval_boost_retention_days` already worked, so they can be calibrated later without a code change. `get_boost_map` now reads config. Kept `MIN_SHOWN=3` by explicit user decision (asked, given the evidence, since real usage cadence is their call, not mine). New tests: `test_min_shown_and_cap_are_config_overridable` (proves the overrides take effect and the cap ceilings net evidence) and `test_retrieval_memory_survives_a_real_venv_upgrade`.
- Updated PROJECT_STATUS.md's "can memory be counted on to improve over time" framing from the previous honest "no" to a qualified yes: it works, survives upgrades, and can never hurt correctness, but its impact is modest by design (only nudges when the same task recurs >= 3 times in a repo) and still unmeasured across sustained multi-session real usage. **1.0.9 phases 9a-9g are all complete; no version bump/tag cut yet.**

## 2026-07-01 (6)

- 1.0.9 phase 9f (`docs/1.0.9_PLAN.md`, the Opus/High destructive phase): uninstaller overhaul. New inverse-of-setup functions in `client_setup.py` -- `remove_claude_code()` (detects the actual registered scope via `claude mcp get`, deregisters it, deletes the slash-command dir), `remove_codex()` (strips only our `# BEGIN/# END neo-localmcp` marked block via new `_strip_marked_block`, leaving the user's own config.toml intact), `remove_claude_desktop()` (manual-only by design), plus `remove_client`/`remove_clients` dispatchers and a new `neo-localmcp remove-client --client X` CLI subcommand. `uninstall.ps1` gained granular switches (`-KeepVenv`, `-KeepMcpb`, `-RemoveConfig`, `-RemoveDatabase`, `-RemoveServers`) while its no-arg default and `-RemoveData` alias reproduce the exact prior behavior (existing tests unaffected). `setup.ps1`'s `Invoke-Uninstall` is now a multi-select over surfaces (Claude Code / Codex / Claude Desktop / CLI+local-state) with a per-category keep/delete checklist for the CLI local state, each destructive data category (config, database) behind its own typed-`DELETE` gate, and the database prompt explicitly stating it is ONE shared file across every indexed repo (per `config.py:110-111`).
- **Found and fixed a real, silent data-loss bug via live testing (exactly what the user asked to test themselves).** First cut collected the granular choices into a PowerShell array of `"-Switch"` strings and called `& $UninstallScript @switches`. Array-splatting into a PowerShell script binds elements as POSITIONAL VALUES, not switch parameters, so every switch was silently dropped and `uninstall.ps1` ran on its defaults -- a user could type `DELETE` to wipe their shared database and it would be preserved with no error. Isolated the mechanism with a minimal repro (`& $t @arr` -> `RemoveConfig=False`; `& $t @hashtable` -> `True`), then switched the wizard to a hashtable splat (`$switchParams = @{}; $switchParams["RemoveDatabase"] = $true`). Re-verified live end-to-end against an isolated `NEO_LOCALMCP_HOME` named `.neo-localmcp` (uninstall.ps1's pre-existing name guard correctly rejected a differently-named test dir along the way): typing `DELETE` at both gates now removes venv/bin/mcpb/config/database/servers and leaves the app home empty, while a keep-everything run preserves all of it. Note for future: the 9e installer-surface code splats an array into an external `.cmd` (`& $Cmd @setupArgs`) which is fine -- raw string args to an executable -- but any splat into a PowerShell script with typed params must be a hashtable.
- New tests: `tests/test_client_removal.py` (9 unit tests -- marked-block strip/roundtrip, setup-then-remove codex, dry-run safety, claude-code dir removal, dispatcher `all` expansion) and two in `tests/test_upgrade_cycle.py` (`test_uninstall_granular_switches_keep_and_delete_independently` for the flags, and `test_setup_wizard_uninstall_surface_actually_deletes_selected_data` which drives the real wizard over stdin -- the only test that actually exercises the splat and would have caught the bug above; the direct-flag test does not).
- 9g (P5 retrieval-boost tuning + upgrade-persistence guarantee) not started -- the last Opus/High phase.

## 2026-07-01 (5)

- 1.0.9 phase 9e (`docs/1.0.9_PLAN.md`): `setup.ps1`'s `Invoke-Install` no longer offers one all-or-nothing "register clients? [Y/n]" prompt. It now asks a multi-select (Claude Code / Codex CLI-Desktop / Claude Desktop / all / none), then shows the exact path(s) each selected surface will touch -- pulled live from `client_setup.py`'s existing `client_status()` via `neo-localmcp clients` (new `Get-ClientStatus`/`Show-SurfacePreview` helpers), not re-derived or hardcoded -- before a final confirm. Claude Desktop's preview correctly states it cannot be automated and points at the 9c versioned-but-fixed-name local `.mcpb` copy. Verified live end-to-end against an isolated `NEO_LOCALMCP_HOME`: drove the wizard with scripted stdin (`1` install, `y` force-repair to actually reach the offer, `1,3` to select Claude Code + Claude Desktop, `n` to decline the final confirm) and confirmed the real preview text (live slash-command path, live `claude mcp add` command reflecting the actual installed server launcher, live Desktop `.mcpb` path) rendered correctly and that declining left `~/.claude` untouched -- deliberately avoided answering "y" in this test since `setup_claude_code()` writes to the real, non-isolated `~/.claude/commands/`.

## 2026-07-01 (4)

- 1.0.9 phases 9a-9d (onboarding polish, see `docs/1.0.9_PLAN.md`): `docs/AGENT_INTEGRATION.md` (copy-pasteable downstream `CLAUDE.md`/`AGENTS.md` snippet) plus a README pointer to it; `install.ps1` now checks for `ollama.exe`, starts it headless if present but not running, and reports found/missing status for the configured `fast_model`/`summary_model` (never auto-pulls); `.mcpb` packaging is now versioned (`packages/claude-desktop/neo-localmcp-v<version>.mcpb`, built via updated `scripts/build-mcpb.ps1`/`.sh`), with `install.ps1` still copying it to a fixed-name local copy at `~/.neo-localmcp/neo-localmcp.mcpb` so Desktop-extension instructions don't need to track a version suffix; the post-install indexing reminder is now its own called-out block instead of one bullet in a generic "Next:" list.
- Found and fixed a real bug during verification, not just in principle: the first cut of the 9b Ollama check called `ollama list`/`Start-Process ollama serve` with no timeout, and `tests/test_upgrade_cycle.py::test_install_then_uninstall_while_server_is_live` caught it hanging `install.ps1` past the test's 300s subprocess timeout. Root cause was an unbounded external-process call, not user interaction (`install.ps1` has no `Read-Host` prompts at all — only the `setup.ps1` wizard does). Fixed with an `Invoke-BoundedExe` helper that runs each Ollama call as a PowerShell job with an explicit 10-15s cap, wrapped the whole check in try/catch, and re-verified: `install.ps1` alone completes in well under 30s against a clean app-home, and the previously-hanging test now passes in ~45s. This is the install-time analogue of the "Ollama failures never block deterministic behavior" rule `ollama_client.py` already enforces at request time — worth remembering for any future installer code that shells out to an external, potentially-unresponsive executable.
- 9e-9g (installer surface selection, uninstaller overhaul, P5 retrieval-boost tuning) not started.

## 2026-07-01 (3)

- Added `setup.ps1`, an interactive install/uninstall wizard for Windows (repo-root convenience script, not part of the installed package — no version bump needed, nothing about what 1.0.8 actually installs changed). Detects current state (installed version vs. source version, legacy pre-1.0.8 `venvs/` leftovers, whether the Claude Desktop extension is present) and presents a plain-text menu (Install/Upgrade, Uninstall, Show status, Exit) instead of requiring the user to remember `install.ps1`/`uninstall.ps1` flags. Wraps those scripts rather than reimplementing their logic — they remain directly callable for scripting/CI. Uninstall's `-RemoveData`-equivalent path requires typing the literal word `DELETE` to proceed (not just y/n), since it destroys all indexed repos, retrieval memory, and cached summaries. The wizard explicitly cannot manage the Claude Desktop extension (a separate thing installed through Claude's own UI, not a filesystem the CLI scripts own) — it only detects and warns if present, per the user's explicit choice after describing Claude's own extension-uninstall UI as unreliable for them ("9/10 times it just doesn't work... I end up restarting my PC"). Found and fixed a real bug during live interactive testing: the "register with Claude Code/Codex?" prompt was skipped by an early `return` on the same-version-already-installed path, causing a leftover piped answer to be misinterpreted as a menu choice on the next loop iteration. Verified live: status detection, the same-version skip path, and the typed-confirmation safety gate (a wrong phrase correctly cancels, leaving the venv/config/database untouched — confirmed by direct filesystem check afterward, catching my own `ls` mistake of forgetting `-a` for the dotfile venv directory along the way).

## 2026-07-01 (2)

- Released 1.0.8: live post-restart testing of 1.0.7 through the connected Claude Desktop extension surfaced two more real findings, plus a design change from the user in response. (1) A stale pre-1.0.7 cached section-summary (1000-char keyword truncation, timestamped before the P7a fix) was served as `cached: true` — expected behavior since cache invalidation is keyed on source-file content hash, not on a code version, but worth knowing operationally: 1.0.7's bounding fix does not retroactively clean already-cached garbage. (2) Ollama auto-start silently used the wrong models directory after a fresh PC restart: the user's `OLLAMA_MODELS` env var (pointing at a large external drive) was not inherited by the server process spawned under Claude Desktop's process tree, so it fell back to a near-empty default path and reported `model_missing` with no error — a real bug in `ollama_client.py`'s `start_service()`, not yet fixed, tracked as a 1.0.9+ follow-up.
- The user then asked why this session's repeated `install.ps1` runs spiked C: drive utilization despite Ollama's models living on a different drive entirely. Root cause: 1.0.7's P7c side-by-side venv scheme still created a brand-new timestamped `venvs/<timestamp>-<pid>` directory on every install run (8-10+ times across the session) and did a full `pip install --force-reinstall` of ~35 dependencies into each one — thousands of small file writes per build, all on `C:\Users\Neel\.neo-localmcp`, compounded by Windows Defender scanning each newly-written file. This diagnosis led directly to 1.0.8's redesign.
- **Single-venv-per-version redesign** (Windows only; macOS `install.sh`/`uninstall.sh` still on the old scheme, same 7d gap as before): now that 1.0.7's P7b/P7c graceful-stop mechanism can reliably stop a running server before touching its files, the original justification for "always create a new directory, never touch an existing one" (dodging a locked-file failure against a live server) no longer applies. `install.ps1` now maintains exactly one venv at `~/.neo-localmcp/.venv-nlm-v<version>`: reinstalling the same version is a fast no-op (~1.2s, verified live, vs. 40-90s for a full rebuild) unless `-Repair` is passed; installing a different version gracefully stops any running server, removes the old venv (including sweeping the entire legacy pre-1.0.8 `venvs/` side-by-side layout wholesale on first upgrade), and builds fresh. `uninstall.ps1` updated to match. `-KeepPreviousVenvs`/`-Upgrade` removed (no longer meaningful — there is never more than one venv to retain). Verified live end-to-end against the real `~/.neo-localmcp`: a real upgrade from a leftover 1.0.7 `venvs/` folder (2 stale dirs, ~158MB) correctly swept the whole legacy folder and landed on a single `.venv-nlm-v1.0.8`. New tests: `test_reinstalling_same_version_is_a_fast_noop_not_a_rebuild` (proves no rebuild via directory ctime / python.exe unchanged), `test_repair_flag_forces_rebuild_of_same_version` (proves `-Repair` actually rebuilds, using file ctime since Windows' `venv` module copies `python.exe` preserving the source interpreter's mtime, not build time — a real test-design trap worth remembering).

## 2026-07-01

- Released 1.0.7 after a full-tilt live cold-start test against AntiNotepad (Ollama killed, memory nuked, all 12 MCP tools exercised end-to-end) surfaced two real problems the automated suite hadn't caught: an unbounded Ollama section-summary generation (106s runaway on a code-heavy section, ignoring its own "at most 8 keywords" instruction, polluting the FTS index with garbage) and an upgrade path that required force-killing a live server to unlock its venv, which repeatedly left DWM ghost window-frame artifacts on the host desktop.
- P7a: bounded section-summary generation with `num_predict` (config: `ollama.section_summary_num_predict`, default 400), covering both the summary and keywords portions with one cap. A response that hits the cap is flagged `truncated: true` and skipped from caching/FTS-indexing entirely rather than silently poisoning the index. Added a parsing-layer defense independent of the cap: keywords are capped by comma-count, not character count. Removed duplicate `installed_models` blobs from every Ollama-touching response (summarize whole-file, summarize section, ranking-assisted context).
- P7b: added `neo_localmcp/lifecycle.py` — a cross-process graceful-stop mechanism. Each server self-registers on startup and runs a daemon thread polling a per-PID stop-file; on trigger it does best-effort cleanup and calls `os._exit(0)`, a process-initiated exit that releases file locks and tears down cleanly, unlike an external terminate (on Windows, `SIGTERM` via `os.kill` *is* `TerminateProcess` — the exact mechanism that caused the ghost frames — and FastMCP installs no signal handlers to intercept it). New CLI commands `neo-localmcp servers` (list) and `neo-localmcp stop` (`--pid`/`--all`/`--match-executable`/`--timeout`/`--no-force`), CLI-only by design so a remote MCP client cannot stop servers. `doctor` now reports `running_servers`.
- P7c: rewired `install.ps1`/`uninstall.ps1` to request a graceful stop before touching venv files, with the old force-kill-on-lock-error behavior kept only as an explicit, clearly-messaged last resort for the one-time case of a pre-1.0.7 server that predates the stop watcher. `install.ps1` now prunes old side-by-side venv directories after a successful install (`-KeepPreviousVenvs`, default 1) instead of accumulating them forever. Found and fixed a real Windows quirk during live verification: even after a process is confirmed exited, Windows can hold its DLL file lock for a moment longer (deferred handle release / AV scan-on-close), so both scripts now retry a removal briefly before treating it as a genuine lock. Verified live: a real 1.0.7 server was spawned, `uninstall.ps1` was run while it was still running, and it stopped gracefully with no lock error and no force-kill — the exact failure mode that motivated this work, now closed. Automated as `tests/test_upgrade_cycle.py` (Windows-only, real install/spawn/uninstall subprocess integration test).
- 7d (macOS `install.sh`/`uninstall.sh` parity) explicitly deferred by user decision — not started, not forgotten; tracked in `docs/1.0.7_PLAN.md`.
- P5 (the 1.0.6 implicit-feedback retrieval boost) remains deferred pending real usage mileage, unrelated to this release's scope.

## 2026-06-30

- Added complete manifest accounting, safe deletion pruning, metadata-first freshness checks, clone/worktree-safe identity, and content-addressed summary replacement.
- Added budgeted multi-file source excerpts and a single batched repository search to reduce repeated agent reads.
- Replaced direct Ollama generation with purpose-aware readiness, warm-up, bounded recovery, busy handling, local ownership, and deterministic fallback.
- Reduced the MCP surface, added workspace-root resolution, unified Codex configuration, and added Claude Desktop MCPB packaging scaffolding.
- Added Windows/macOS install lifecycle helpers, built a validated 1.0.0 Claude Desktop MCPB bundle, and expanded the regression suite to 17 passing tests on Windows.
- Verified live `qwen3:8b` cold-model warm-up, ready-state reporting, bounded inference, and combined deterministic/Ollama context output on the Windows 4080 host.
- Added explicit warm-timeout classification so model loading delays cannot be mislabeled as supervisor lock contention.
- Verified automatic localhost recovery from a stopped Ollama service through service start, `qwen3:8b` warm-up, and ready-state confirmation.
- Changed installers to side-by-side versioned virtual environments and stable MCP launchers so active Windows clients cannot lock an in-place upgrade.
- Added idempotent Claude Code registration migration from the legacy project-local venv command to the stable user-scope launcher.
- Completed a real side-by-side Windows install and migrated Claude Code plus Codex to the stable server launcher.
- Fixed Windows MCP worker Unicode output by enforcing UTF-8 across the subprocess protocol, added a CP1252 regression test, and released the rebuilt Claude Desktop bundle as 1.0.1.
- Prepared 1.0.2 from a live Claude f4-plan trial: exact document paths now outrank keyword noise, colon focus ignores filler terms while preserving milestone tokens such as `f4`, read-only lookup no longer runs Git metadata probes, and omitted Ollama `:latest` tags resolve to installed models.
- Expanded the 1.0.2 MCP bundle so `/doctor`, `/status`, `/index`, `/refresh`, `/summarize`, and exact approved `/apply-patch` operations have dedicated tools instead of being guessed as unrelated calls or requiring a CLI fallback.
- Released 1.0.3 after live Claude testing exposed a stdio-only deadlock: helper Git processes inherited the MCP protocol input handle. Helper commands now use a closed stdin, and a real client/server stdio regression test covers refresh, lookup, and patch validation.
- Corrected context selection so explicit paths always lead, explanation/context intents follow ranked docs rather than an unconditional source-first pass, filename token matches outrank repeated cross-document references, and stale line hints clamp safely to EOF.
- Verified the complete AntiNotepad MCP suite live: repository tools return in under two seconds, the f4 plan ranks first for the reported query, and full-file Ollama summarization completes successfully in about 23 seconds.
- Investigated a reported per-call process leak with a controlled 30-call single-session test: both `conhost.exe` and neo-localmcp-related process counts returned exactly to baseline after disconnect. Persistent Python processes map to active MCP client sessions, including Windows venv shim/base-interpreter pairs, rather than tool calls.
- Hardened all short-lived Git, context-worker, and client-setup subprocesses with closed stdin and Windows `CREATE_NO_WINDOW` so helpers cannot consume MCP protocol input or allocate transient console hosts.
- Released the subprocess hardening as 1.0.4. A supplied WER dump was also reviewed and identified as a kernel blue screen (`0x7E` / kernel access violation), not a Python, conhost, or neo-localmcp application crash.
- Live AntiNotepad testing against the f4 plan doc found two compounding 1.0.4 gaps: Markdown had no symbol extraction at all (no addressable unit smaller than a whole file), and the context excerpt centerer fell back to line 1 for any file with no symbol hits — so a correctly-retrieved 1000+ line doc still opened at its header instead of the matched topic.
- Released 1.0.5: added deterministic ATX heading extraction for Markdown (fenced and indented code blocks are excluded so example markdown cannot mint phantom sections), heading symbols are now ranked with an exact-milestone boost (e.g. `f4.7`) so they outrank incidental code-symbol collisions on generic words, and `context_prepare` centers excerpts on the best-matching heading's real section range instead of the file opening. Bumped `INDEXER_VERSION` to `1.1.0` so existing repositories rebuild automatically. Added 17 regression tests covering heading extraction, fenced-block immunity, section-vs-file-opening excerpts, heading-beats-code-symbol ranking, filename-anchor-plus-section retrieval, and boundary updates on heading insert/removal; the determinism suite was re-verified with heading symbols present.
- Released 1.0.6: query/result metadata recording moved from a hidden env var (`NEO_LOCALMCP_RECORD_CONTEXT_QUERIES`) to an explicit `memory.record_context_queries` config flag (on by default), with `repo_status`/`doctor` now reporting `query_recording_enabled`, `recorded_queries`, and `last_query_recorded_at`; only compact metadata (terms, shown sections, tool version) is stored, not the full response, with retention pruning. Added an implicit-feedback loop: `prepare_context` returns a `retrieval_id`, and passing it to a follow-up `file_excerpts` call records whether the pulled range overlapped the suggested section (followed) or landed elsewhere (corrected). Accumulated evidence produces a small, capped (`RETRIEVAL_BOOST_CAP=8`), recency-gated (`RETRIEVAL_BOOST_MIN_SHOWN=3`, 90-day window) ranking nudge — far below any structural signal (a heading milestone match alone is +60), so memory can only break near-ties, never override real structural evidence; verified determinism still holds identically once a boost is active. Added Ollama-backed per-heading section summaries (`summarize_file(..., heading=...)`), cached by whole-file content hash and indexed as supplemental FTS text; summaries never determine a heading's line boundaries, and an Ollama failure on that path leaves deterministic retrieval fully unaffected. 20 new regression tests; full suite green except the (expected, pre-rebuild) packaged-bundle staleness check.
