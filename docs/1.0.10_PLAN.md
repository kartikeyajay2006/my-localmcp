# neo-localmcp 1.0.10 Cross-Platform Setup v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the Windows-first installer family with one Python 3.12+ lifecycle that safely installs, reinstalls, uninstalls, clean-installs, migrates, reconnects clients, preserves memory by default, and verifies the resulting MCP endpoint on Windows, macOS, and Linux.

**Architecture:** `setup_v2.py` is the only lifecycle entrypoint during development and delegates to focused modules under `neo_localmcp/installer/`. Shared Python code owns policy; cross-platform libraries and small platform adapters handle executable paths, process details, permissions, and filesystem replacement. A candidate venv is built and verified before any healthy runtime is replaced.

**Tech Stack:** Python 3.12+, stdlib `argparse`/`venv`/`pathlib`/`json`/`subprocess`, `psutil` for cross-platform process ownership and trees, existing MCP/FastMCP client stack, pytest, isolated temporary homes, and real lifecycle smoke tests on macOS/Windows/Linux.

## Global Constraints

- `setup_v2.py` is the only supported install/reinstall/uninstall entrypoint until it is renamed to `setup.py` after cross-platform parity.
- The installed executable remains `neo-localmcp`; no `neo` alias is created.
- Python 3.12 or newer is required; the installer never installs system Python.
- Installation source is the local checkout containing `setup_v2.py`.
- `install` reuses durable data; `reinstall` replaces only the runtime; `uninstall` removes only the runtime by default.
- `install --clean` deletes the entire validated managed root and then installs fresh.
- `uninstall --delete-memory --yes` deletes the entire validated managed root and does not reinstall.
- Full-root deletion always requires interactive confirmation or the explicit `--yes` non-interactive flag.
- Durable data is never deleted by default.
- Neo unloads models it used but never kills a shared Ollama daemon.
- Process termination requires verified Neo ownership; never kill by process name alone.
- Avoid OS-specific files unless they materially improve reliability, stability, or functionality.
- When implementation depends on a library/tool API, consult Context7 first when available, then the product's primary official documentation; use third-party sources only when primary documentation is silent and label the inference.
- Every command and subcommand supports `-h` and `--help`.
- Implementation is test-first, each phase ends in a focused commit, and old installers remain available until real Windows parity is verified.

## Scope and sequencing

This plan is intentionally sequential because paths, state, runtime transactions,
and ownership are safety boundaries consumed by every later phase. Tasks 1-13 can
be built and verified on macOS now. Task 14 uses Windows x64 CI as its durable gate
and any convenient temporary Windows VM only for targeted live smoke testing. Task
15 adds Linux automation. The old scripts are retired only in Task 16 after all
gates pass.

## Documentation verification protocol

Before implementing an unfamiliar or version-sensitive API:

1. Resolve and query the official/high-reputation Context7 library entry.
2. Confirm safety-sensitive behavior against the upstream project's official docs
   or repository documentation when Context7 excerpts leave ambiguity.
3. Record the relevant documentation URL in task notes or a code comment only
   where it explains a non-obvious safety decision.
4. If primary documentation does not answer the question, state the inference and
   add a test that makes the assumed behavior explicit.

Initial documentation checks completed while writing this plan:

- psutil officially documents PID/create-time identity, `exe()`, `cmdline()`,
  descendant traversal, `terminate()`, `kill()`, `wait_procs()`, and handling of
  `NoSuchProcess`/`AccessDenied`: https://psutil.readthedocs.io/
- Ollama officially documents model unload through `POST /api/generate` with the
  model name and `keep_alive: 0`, without stopping the daemon:
  https://github.com/ollama/ollama/blob/main/docs/api.md

## Target file map

### New lifecycle files

- `setup_v2.py` — thin Python-version check and lifecycle CLI dispatcher.
- `neo_localmcp/installer/__init__.py` — stable public installer interface.
- `neo_localmcp/installer/types.py` — enums and immutable result/state records.
- `neo_localmcp/installer/paths.py` — canonical layout and destructive-path validation.
- `neo_localmcp/installer/state.py` — installation-state detection and metadata transitions.
- `neo_localmcp/installer/migration.py` — recognized legacy-layout migration.
- `neo_localmcp/installer/output.py` — exact terminology, progress events, summaries, and confirmations.
- `neo_localmcp/installer/processes.py` — owned-process inventory, tree shutdown, and lock diagnostics.
- `neo_localmcp/installer/ollama.py` — identify and unload Neo-used models without stopping Ollama.
- `neo_localmcp/installer/runtime.py` — candidate venv build, validation, atomic promotion, rollback, and deletion.
- `neo_localmcp/installer/clients.py` — registration snapshots, removal, restoration, and verification.
- `neo_localmcp/installer/verification.py` — CLI, package, MCP handshake, path, client, and doctor checks.
- `neo_localmcp/installer/operations.py` — install/reinstall/uninstall/clean-install orchestration.

### New tests

- `tests/installer/test_paths.py`
- `tests/installer/test_state.py`
- `tests/installer/test_migration.py`
- `tests/installer/test_output.py`
- `tests/installer/test_processes.py`
- `tests/installer/test_ollama_shutdown.py`
- `tests/installer/test_runtime.py`
- `tests/installer/test_clients.py`
- `tests/installer/test_verification.py`
- `tests/installer/test_operations.py`
- `tests/installer/test_setup_cli.py`
- `tests/installer/test_macos_lifecycle.py`
- `tests/installer/test_windows_lifecycle.py`
- `tests/installer/test_linux_lifecycle.py`

### Existing files modified

- `pyproject.toml` — Python floor, `psutil`, packaging, and version.
- `neo_localmcp/config.py` — canonical durable paths and compatibility overrides.
- `neo_localmcp/lifecycle.py` — richer process registration under the new cache layout.
- `neo_localmcp/ollama_client.py` — canonical state paths and reusable model-unload primitive.
- `neo_localmcp/client_setup.py` — explicit executable injection and registration snapshots.
- `neo_localmcp/cli.py` — remove lifecycle ambiguity from the installed `setup` command.
- `neo_localmcp/tools.py` — update installed-command help and canonical paths.
- `tests/conftest.py` — isolated canonical layout fixture.
- `tests/test_upgrade_cycle.py` — retain legacy-script coverage until retirement.
- `README.md`, `AGENTS.md`, `PROJECT_STATUS.md`, `PROJECT_NOTES.md`, `docs/ARCHITECTURE.md` — document final behavior and verified platforms.
- `neo_localmcp/__init__.py`, `packages/claude-desktop/mcpb/manifest.json` — synchronized 1.0.10 version bump at release closeout.
- `setup.ps1`, `install.ps1`, `uninstall.ps1`, `install.sh`, `uninstall.sh` — retained until parity, then removed in the final retirement commit.

---

### Task 1: Establish the Python 3.12 installer package and test harness

**Purpose:** Create importable boundaries without changing installation behavior.

**Model / effort:** GPT-5.5 Medium.

**Files:**
- Create: `neo_localmcp/installer/__init__.py`
- Create: `neo_localmcp/installer/types.py`
- Create: `tests/installer/__init__.py`
- Modify: `pyproject.toml`
- Modify: `tests/conftest.py`

**Interfaces:**
- Produces: `Operation`, `InstallStateKind`, `OperationStatus`, `OperationResult`, and `DetectedState`.
- Produces: `isolated_app_home` fixture using `NEO_LOCALMCP_HOME` and canonical directories.

- [x] **Step 1: Write failing contract tests for enums and immutable results**

```python
from neo_localmcp.installer.types import Operation, OperationResult, OperationStatus


def test_operation_result_is_immutable():
    result = OperationResult(Operation.INSTALL, OperationStatus.SUCCEEDED, (), ())
    with pytest.raises(dataclasses.FrozenInstanceError):
        result.status = OperationStatus.FAILED
```

- [x] **Step 2: Run the focused test and confirm the package is missing**

Run: `python -m pytest tests/installer -q`

Expected: collection fails with `ModuleNotFoundError: neo_localmcp.installer`.

- [x] **Step 3: Add frozen types with exact values**

```python
class Operation(str, Enum):
    INSTALL = "install"
    REINSTALL = "reinstall"
    UNINSTALL = "uninstall"


class InstallStateKind(str, Enum):
    ABSENT = "absent"
    DATA_ONLY = "data-only"
    HEALTHY = "healthy"
    BROKEN_RUNTIME = "broken-runtime"
    LEGACY_LAYOUT = "legacy-layout"
    PARTIAL_OPERATION = "partial-operation"


class OperationStatus(str, Enum):
    SUCCEEDED = "succeeded"
    FAILED = "failed"
    CANCELLED = "cancelled"


@dataclass(frozen=True)
class OperationResult:
    operation: Operation
    status: OperationStatus
    actions: tuple[str, ...]
    warnings: tuple[str, ...]
```

- [x] **Step 4: Raise the package floor and add the process dependency**

Set `requires-python = ">=3.12"` and add `psutil>=5.9,<8` to runtime dependencies. Do not bump the product version yet.

- [x] **Step 5: Add an isolated-home fixture and run baseline regression tests**

Run: `python -m pytest tests/installer -q && python -m pytest -q && python -m compileall -q neo_localmcp`

Expected: installer tests pass; the existing suite either passes under Python 3.12 or identifies explicit compatibility work that must be fixed in this task.

- [x] **Step 6: Commit the foundation**

```bash
git add pyproject.toml neo_localmcp/installer tests/conftest.py tests/installer
git commit -m "build: establish setup v2 installer foundation"
```

**Gate:** No lifecycle mutation exists yet; all existing commands still behave unchanged.

**Verified 2026-07-01:** focused installer tests passed; full suite passed with
93 tests and 6 platform skips; `compileall` passed. Verification also exposed and
fixed the existing macOS-incompatible `sed \\s` version parser in
`scripts/build-mcpb.sh`; its regression test passes and the 1.0.9 MCPB snapshot was
rebuilt to retain byte-for-byte package coverage.

---

### Task 2: Define the canonical managed layout and deletion guard

**Purpose:** Make every path calculation and destructive decision flow through one testable object.

**Model / effort:** GPT-5.5 High because path validation is a data-loss boundary.

**Files:**
- Create: `neo_localmcp/installer/paths.py`
- Create: `tests/installer/test_paths.py`

**Interfaces:**
- Produces: `ManagedPaths.from_environment() -> ManagedPaths`.
- Produces: `ManagedPaths.validate_destructive_root() -> None`, raising `UnsafeManagedRoot`.
- Consumed later by every installer module; no later task constructs managed paths manually.

- [x] **Step 1: Write table-driven layout tests for Windows and POSIX semantics**

Assert exact members: `root`, `venv`, `memory`, `sqlite`, `config`, `clients`, `logs`, `cache`, `install_metadata`, `process_registry`, `candidate_venv`, `cli_executable`, and `server_executable`.

```python
def test_posix_layout(tmp_path):
    paths = ManagedPaths(root=tmp_path / ".neo-localmcp", platform="posix")
    assert paths.cli_executable == paths.venv / "bin" / "neo-localmcp"
    assert paths.install_metadata == paths.config / "install.json"
    assert paths.process_registry == paths.cache / "processes"
```

- [x] **Step 2: Write refusal tests**

Cover empty input, `/`, the current home directory, a symlink resolving to home, a root named something other than `.neo-localmcp`, and a root whose parent cannot be resolved. Include a test-only `allow_test_root=True` path for temporary integration homes.

- [x] **Step 3: Run tests and confirm missing interfaces**

Run: `python -m pytest tests/installer/test_paths.py -q`

Expected: import failure.

- [x] **Step 4: Implement `ManagedPaths` and `UnsafeManagedRoot`**

Use `Path.expanduser().resolve(strict=False)`. Keep platform branching limited to venv executable locations and `.exe` suffixes. `ensure_directories()` creates durable directories but never creates `venv/`.

- [x] **Step 5: Run focused and full tests**

Run: `python -m pytest tests/installer/test_paths.py -q && python -m pytest -q`

Expected: all pass.

- [x] **Step 6: Commit**

```bash
git add neo_localmcp/installer/paths.py tests/installer/test_paths.py
git commit -m "feat: define safe managed filesystem layout"
```

**Gate:** A reviewer can prove no installer deletion accepts an unvalidated root.

**Verified 2026-07-01:** 11 path-model tests passed; full suite passed with 104
tests and 6 platform skips; `compileall` passed. The bundle snapshot was rebuilt
and its byte-for-byte package check passed.

---

### Task 3: Move runtime configuration to canonical durable paths

**Purpose:** Make the application itself consume the layout the installer will manage.

**Model / effort:** GPT-5.5 High because every memory/config consumer is affected.

**Files:**
- Modify: `neo_localmcp/config.py`
- Modify: `neo_localmcp/ollama_client.py`
- Modify: `neo_localmcp/lifecycle.py`
- Modify: `tests/conftest.py`
- Create: `tests/installer/test_config_paths.py`
- Modify: existing tests that assert flat paths.

**Interfaces:**
- Produces: `CONFIG_DIR`, `CONFIG_PATH`, `SQLITE_DIR`, `DEFAULT_DB_PATH`, `CACHE_DIR`, and `PROCESS_REGISTRY_DIR` derived dynamically from `APP_DIR`.
- Preserves: `NEO_LOCALMCP_CONFIG` and configured `memory.db_path` overrides.

- [ ] **Step 1: Write failing canonical-path tests**

```python
def test_default_paths_are_durable_subdirectories(isolated_app_home):
    assert config.CONFIG_PATH == isolated_app_home / "config" / "config.yaml"
    assert config.db_path() == isolated_app_home / "sqlite" / "repo-context.sqlite"
```

Also assert supervisor state moves under `cache/ollama/` and lifecycle registry under `cache/processes/servers/`.

- [ ] **Step 2: Run focused tests and capture old flat-path failures**

Run: `python -m pytest tests/installer/test_config_paths.py tests/test_lifecycle.py tests/test_ollama_client.py -q`

Expected: canonical assertions fail against current flat paths.

- [ ] **Step 3: Implement dynamic path helpers**

Avoid import-time constants that become stale when tests patch `APP_DIR`. Add functions such as `config_path()`, `default_db_path()`, and `cache_path(*parts)`; retain constants only where compatibility requires them.

- [ ] **Step 4: Update consumers and fixtures**

Replace direct `APP_DIR / "repo-context.sqlite"`, `APP_DIR / "servers"`, and supervisor-state construction with the helpers. Ensure parent directories are created before writes.

- [ ] **Step 5: Run path, memory, lifecycle, and full suites**

Run: `python -m pytest tests/installer/test_config_paths.py tests/test_lifecycle.py tests/test_ollama_client.py tests/test_retrieval_memory.py -q && python -m pytest -q`

Expected: all pass with canonical paths.

- [ ] **Step 6: Commit**

```bash
git add neo_localmcp/config.py neo_localmcp/ollama_client.py neo_localmcp/lifecycle.py tests
git commit -m "refactor: separate durable data from runtime paths"
```

**Gate:** A normal application run creates no new flat config/database/process-state files.

---

### Task 4: Detect installation state and persist lifecycle metadata

**Purpose:** Let every operation begin from an explicit, explainable machine state and recover from interruption.

**Model / effort:** GPT-5.5 High.

**Files:**
- Create: `neo_localmcp/installer/state.py`
- Create: `tests/installer/test_state.py`

**Interfaces:**
- Produces: `detect_state(paths: ManagedPaths) -> DetectedState`.
- Produces: `begin_operation(...)`, `complete_operation(...)`, and `fail_operation(...)` using atomic JSON replacement.
- Metadata schema includes `schema_version`, `operation`, `status`, `source_version`, `started_at`, `completed_at`, `runtime_python`, `clients`, and `error`.

- [ ] **Step 1: Write one fixture/test per state kind**

Construct absent, data-only, healthy, broken-runtime, legacy-layout, and partial-operation homes. A healthy runtime must have a runnable interpreter plus importable package marker; directory existence alone is insufficient.

- [ ] **Step 2: Write metadata atomicity and corruption tests**

Assert writes use a sibling temporary file and `os.replace`; malformed metadata returns a partial/broken state with a warning rather than being overwritten silently.

- [ ] **Step 3: Run tests and verify failure**

Run: `python -m pytest tests/installer/test_state.py -q`

Expected: import failure.

- [ ] **Step 4: Implement state detection and metadata transitions**

Use deterministic reason codes in `DetectedState.details`, including `venv_missing`, `python_missing`, `package_missing`, `metadata_in_progress`, and `legacy_paths_present`.

- [ ] **Step 5: Run focused/full tests and commit**

Run: `python -m pytest tests/installer/test_state.py -q && python -m pytest -q`

```bash
git add neo_localmcp/installer/state.py tests/installer/test_state.py
git commit -m "feat: detect and record installation lifecycle state"
```

**Gate:** Re-running after a simulated crash reports the interrupted operation and preserves evidence.

---

### Task 5: Implement explicit legacy-layout migration

**Purpose:** Preserve all existing user memory/config while converging Windows and Unix installs on the new layout.

**Model / effort:** GPT-5.5 High.

**Files:**
- Create: `neo_localmcp/installer/migration.py`
- Create: `tests/installer/test_migration.py`

**Interfaces:**
- Produces: `plan_migration(paths) -> MigrationPlan` without mutation.
- Produces: `apply_migration(plan) -> MigrationResult` with collision handling and rollback journal.
- Recognizes: flat `config.yaml`, flat SQLite/WAL/SHM files, `.venv-nlm-v*`, `venvs/*`, `venv/`, `bin/`, `current-venv.txt`, `servers/`, supervisor files, and local `.mcpb`.

- [ ] **Step 1: Write migration-plan tests for every recognized legacy layout**

Assert durable moves separately from disposable-runtime cleanup. A plan must never classify unknown files for deletion.

- [ ] **Step 2: Write collision and rollback tests**

If both old and new config/database files exist, preserve both, stop, and print a resolvable conflict. If a move fails mid-migration, restore already-moved files from the journal.

- [ ] **Step 3: Run tests and verify missing implementation**

Run: `python -m pytest tests/installer/test_migration.py -q`

- [ ] **Step 4: Implement dry planning, apply, and journal cleanup**

For SQLite, move the database with its `-wal` and `-shm` companions only while Neo processes are stopped. Do not open or rewrite the database during migration.

- [ ] **Step 5: Verify byte/data preservation**

Run: `python -m pytest tests/installer/test_migration.py tests/test_retrieval_memory.py -q`

Expected: seeded retrieval rows and file bytes survive migration.

- [ ] **Step 6: Commit**

```bash
git add neo_localmcp/installer/migration.py tests/installer/test_migration.py
git commit -m "feat: migrate legacy installs without losing durable data"
```

**Gate:** Migration is previewable, repeatable, collision-safe, and idempotent.

---

### Task 6: Standardize progress output, confirmation, and operation summaries

**Purpose:** Lock terminology before destructive orchestration is added.

**Model / effort:** GPT-5.5 Medium.

**Files:**
- Create: `neo_localmcp/installer/output.py`
- Create: `tests/installer/test_output.py`

**Interfaces:**
- Produces: `Reporter.info/warn/error/action/summary`.
- Produces: `confirm_full_wipe(paths, input_fn, output_fn) -> bool`.
- Produces exact preserved-memory message from the design.

- [ ] **Step 1: Write golden-output tests**

Assert the exact phrases `Uninstall removes the managed runtime. It does not recreate it.`, `Reinstall replaces the managed runtime and preserves memory/data.`, and `Existing neo-localmcp memory detected. Reusing preserved memory/data.`

- [ ] **Step 2: Write confirmation tests**

The prompt lists `venv`, `memory`, `sqlite`, `config`, `clients`, `logs`, and `cache`, requires an exact confirmation phrase interactively, and bypasses prompting only when the caller has already parsed `--yes`.

- [ ] **Step 3: Implement reporter events and deterministic summary rendering**

Return structured events as well as printing them so tests and future JSON output do not scrape terminal strings.

- [ ] **Step 4: Run and commit**

Run: `python -m pytest tests/installer/test_output.py -q`

```bash
git add neo_localmcp/installer/output.py tests/installer/test_output.py
git commit -m "feat: standardize setup lifecycle terminology"
```

**Gate:** No operation module contains user-facing deletion language of its own.

---

### Task 7: Track and stop only Neo-owned process trees

**Purpose:** Release venv and Claude Desktop extension locks without killing unrelated Python, uv, Ollama, or console processes.

**Model / effort:** GPT-5.5 High; this is the highest process-safety phase.

**Files:**
- Create: `neo_localmcp/installer/processes.py`
- Create: `tests/installer/test_processes.py`
- Modify: `neo_localmcp/lifecycle.py`
- Modify: `neo_localmcp/server.py`

**Interfaces:**
- Produces: `OwnedProcess`, `discover_owned_processes(paths, registrations)`, and `stop_owned_processes(..., timeout) -> ShutdownResult`.
- Ownership evidence: registered PID plus matching creation time, executable below managed `venv/`, command line containing a validated Neo extension/runtime path, or descendant of an already verified owned process.

- [ ] **Step 1: Write fake-process graph tests**

Cover a registered server, `uv.exe -> python.exe -> child`, unrelated `uv.exe`, unrelated Python, a reused PID with mismatched creation time, access-denied metadata, and Windows console descendants. Assert only verified trees become targets.

- [ ] **Step 2: Extend server registration tests**

Registry entries must include PID, parent PID, process creation time, executable, command line, source (`managed-runtime` or `client-extension`), version, and start time.

- [ ] **Step 3: Implement discovery using `psutil`**

Keep `psutil` calls behind a `ProcessProvider` protocol so tests use deterministic fakes. Treat missing/access-denied details as insufficient ownership evidence, never permission to kill.

Use the official documented process identity and escalation APIs. Re-query
Context7 `/giampaolo/psutil` before implementation if the installed psutil major
version differs from the planned range.

- [ ] **Step 4: Implement shutdown escalation**

Request existing graceful stop files first, wait, call `terminate()` on verified survivors, wait again, then `kill()` only remaining verified survivors. Kill descendants before parents during forced cleanup.

- [ ] **Step 5: Run focused, existing lifecycle, and live self-stop tests**

Run: `python -m pytest tests/installer/test_processes.py tests/test_lifecycle.py -q`

Expected: unrelated-process assertions pass and existing graceful-stop behavior remains intact.

- [ ] **Step 6: Commit**

```bash
git add neo_localmcp/installer/processes.py neo_localmcp/lifecycle.py neo_localmcp/server.py tests/installer/test_processes.py tests/test_lifecycle.py
git commit -m "feat: stop verified neo-localmcp process trees"
```

**Gate:** A test containing identically named unrelated processes proves they are never terminated.

---

### Task 8: Unload Neo-used Ollama models without stopping the daemon

**Purpose:** Free RAM/file handles during lifecycle operations while respecting shared Ollama ownership.

**Model / effort:** GPT-5.5 Medium.

**Files:**
- Create: `neo_localmcp/installer/ollama.py`
- Create: `tests/installer/test_ollama_shutdown.py`
- Modify: `neo_localmcp/ollama_client.py`

**Interfaces:**
- Produces: `configured_models() -> tuple[str, ...]`.
- Produces: `unload_model(model, timeout) -> ModelUnloadResult`.
- Produces: `unload_neo_models(timeout_per_model=5.0) -> tuple[ModelUnloadResult, ...]`.

- [ ] **Step 1: Write HTTP-contract tests**

Assert each distinct configured model receives a bounded Ollama generate request with `keep_alive: 0`; duplicated fast/summary models unload once. Remote and local configured endpoints are allowed because unload is scoped to a named model, but no daemon process is stopped.

- [ ] **Step 2: Write degradation tests**

Connection refused, timeout, missing model, and HTTP error produce warnings and return promptly. They do not fail default uninstall/reinstall.

- [ ] **Step 3: Implement the reusable primitive and installer wrapper**

Keep network calls bounded using the existing URL/timeouts style. Record models actually requested so the operation summary can report them.

Use the official `POST /api/generate` payload `{"model": <name>, "keep_alive": 0}`;
do not invent a process-level unload mechanism.

- [ ] **Step 4: Update current documentation assertion**

Replace README language saying Neo never unloads automatically with the precise lifecycle exception; ordinary MCP operations still never evict models.

- [ ] **Step 5: Run and commit**

Run: `python -m pytest tests/installer/test_ollama_shutdown.py tests/test_ollama_client.py -q`

```bash
git add neo_localmcp/installer/ollama.py neo_localmcp/ollama_client.py tests/installer/test_ollama_shutdown.py README.md
git commit -m "feat: unload neo-used models during lifecycle changes"
```

**Gate:** Tests prove no code path calls Ollama process termination.

---

### Task 9: Snapshot, remove, restore, and verify client registrations

**Purpose:** Prevent broken client commands after uninstall and reconnect preserved clients after reinstall.

**Model / effort:** GPT-5.5 High because external user configuration is mutated.

**Files:**
- Create: `neo_localmcp/installer/clients.py`
- Create: `tests/installer/test_clients.py`
- Modify: `neo_localmcp/client_setup.py`
- Modify: `tests/test_client_removal.py`
- Modify: `tests/test_distribution.py`

**Interfaces:**
- Produces: `ClientRegistrationRecord` stored under `clients/registrations.json`.
- Produces: `snapshot_clients`, `remove_active_registrations`, `restore_recorded_registrations`, and `verify_registrations`.
- `client_setup` functions accept explicit `server_command: Path`; they do not infer a legacy `bin/` launcher.

- [ ] **Step 1: Write snapshot schema and round-trip tests**

Cover Claude Code, Codex CLI/Desktop, Claude Desktop manual instructions, absent CLIs, dry run, user-owned surrounding config, and missing prior records.

- [ ] **Step 2: Write executable-target tests**

Assert newly written registrations point to `venv/Scripts/neo-localmcp-server.exe` on Windows or `venv/bin/neo-localmcp-server` on POSIX.

- [ ] **Step 3: Refactor current setup/removal helpers for injected paths**

Preserve marked-block behavior and existing inverses. Atomic-write user config files and preserve permissions/newline style where practical.

- [ ] **Step 4: Implement installer-level snapshot/removal/restore**

Default uninstall removes active registrations but retains the records. Reinstall/install restores recorded surfaces after runtime verification. Clean install/full wipe deletes records and requires a new surface selection.

- [ ] **Step 5: Run client suites**

Run: `python -m pytest tests/installer/test_clients.py tests/test_client_removal.py tests/test_distribution.py -q`

Expected: setup/remove round trips preserve unrelated user content byte-for-byte.

- [ ] **Step 6: Commit**

```bash
git add neo_localmcp/installer/clients.py neo_localmcp/client_setup.py tests/installer/test_clients.py tests/test_client_removal.py tests/test_distribution.py
git commit -m "feat: preserve and reconnect client registrations"
```

**Gate:** Default uninstall leaves no active client reference to the removed venv.

---

### Task 10: Build and promote a verified candidate runtime transactionally

**Purpose:** Ensure dependency/build failures never destroy a healthy runtime.

**Model / effort:** GPT-5.5 High; this is the highest filesystem-risk phase.

**Files:**
- Create: `neo_localmcp/installer/runtime.py`
- Create: `tests/installer/test_runtime.py`

**Interfaces:**
- Produces: `build_candidate(paths, source_root, python_executable) -> CandidateRuntime`.
- Produces: `validate_candidate(candidate, expected_version) -> RuntimeValidation`.
- Produces: `promote_candidate(paths, candidate) -> PromotionResult`.
- Produces: `remove_runtime(paths) -> RemovalResult`.

- [ ] **Step 1: Write command-construction unit tests**

Assert `python -m venv`, candidate-python `-m pip install --upgrade pip`, and candidate-python `-m pip install --upgrade --force-reinstall <local checkout>` use argument arrays, never shell strings.

- [ ] **Step 2: Write promotion/rollback tests with fake runtimes**

Cover healthy current runtime, absent runtime, broken runtime, candidate failure, rename failure, old-runtime cleanup failure, and interrupted promotion. The old runtime stays available until the candidate passes validation.

- [ ] **Step 3: Implement candidate build under `cache/runtime-staging/<operation-id>/venv`**

Do not build inside the target `venv/`. Capture subprocess output into lifecycle logs while streaming concise progress to the reporter.

- [ ] **Step 4: Implement candidate validation**

Check interpreter version, installed package version, CLI `--help`, server import, and expected executable files. Do not register clients or touch durable data here.

- [ ] **Step 5: Implement promotion and rollback**

Rename current `venv/` to a rollback path, rename candidate to `venv/`, validate again from its final path, then delete rollback. If final validation fails, restore rollback.

- [ ] **Step 6: Run a real candidate build on macOS in a temporary home**

Run: `python -m pytest tests/installer/test_runtime.py -q -m "not slow"`

Then run the single marked real-build test: `python -m pytest tests/installer/test_runtime.py -q -m slow`

Expected: candidate promotes and reports the local checkout version.

- [ ] **Step 7: Commit**

```bash
git add neo_localmcp/installer/runtime.py tests/installer/test_runtime.py
git commit -m "feat: replace managed runtime transactionally"
```

**Gate:** Injected pip and promotion failures leave the prior healthy venv runnable.

---

### Task 11: Verify the installed CLI, MCP endpoint, paths, clients, and doctor

**Purpose:** Define success from the system’s perspective rather than file-copy completion.

**Model / effort:** GPT-5.5 High.

**Files:**
- Create: `neo_localmcp/installer/verification.py`
- Create: `tests/installer/test_verification.py`

**Interfaces:**
- Produces: `verify_installation(paths, expected_version, expected_clients) -> VerificationReport`.
- Check records have `name`, `required`, `ok`, `details`, and `recovery`.

- [ ] **Step 1: Write one failure test per required check**

Cover Python floor, package version, CLI resolution, CLI startup, server import, bounded MCP initialize handshake, canonical config/database paths, client targets, and `doctor` required checks.

- [ ] **Step 2: Write warning-only checks**

Missing optional Ollama/model availability and manual Claude Desktop installation are warnings, not installation failures.

- [ ] **Step 3: Implement bounded subprocess and MCP probes**

Use the managed Python/executables directly, explicit environment including `NEO_LOCALMCP_HOME`, captured output, and hard timeouts. Clean up probe processes in `finally`.

- [ ] **Step 4: Run focused tests and a real temporary-home verification**

Run: `python -m pytest tests/installer/test_verification.py -q`

Expected: the real probe completes without leaving a registered server PID.

- [ ] **Step 5: Commit**

```bash
git add neo_localmcp/installer/verification.py tests/installer/test_verification.py
git commit -m "feat: verify the installed mcp endpoint end to end"
```

**Gate:** An intentionally broken MCP handshake fails installation verification even when the venv and CLI files exist.

---

### Task 12: Orchestrate install, reinstall, uninstall, and clean install

**Purpose:** Compose approved primitives into the exact lifecycle semantics.

**Model / effort:** GPT-5.5 High; this is the highest destructive-policy phase.

**Files:**
- Create: `neo_localmcp/installer/operations.py`
- Create: `tests/installer/test_operations.py`
- Modify: `neo_localmcp/installer/__init__.py`

**Interfaces:**
- Produces: `install(context, clean=False, assume_yes=False) -> OperationResult`.
- Produces: `reinstall(context) -> OperationResult`.
- Produces: `uninstall(context, delete_memory=False, assume_yes=False) -> OperationResult`.
- `OperationContext` contains paths, source root, Python executable, reporter, process provider, clock, and confirmation function.

- [ ] **Step 1: Write ordered-call tests with fake components**

Assert install order: validate source/Python → detect state → stop owned processes if needed → unload models → snapshot clients → migrate → build/validate candidate → promote → restore clients → verify endpoint → complete metadata → summary.

- [ ] **Step 2: Write semantic matrix tests**

Cover all state/operation combinations. Required assertions:

| Operation | Removes venv | Recreates venv | Preserves durable data | Active clients afterward |
|---|---:|---:|---:|---|
| `install` | only when replacement needed | yes | yes | restored/selected |
| `reinstall` | yes | yes | yes | restored |
| `uninstall` | yes | no | yes | removed |
| `install --clean` | yes | yes | no | newly selected only |
| `uninstall --delete-memory` | yes | no | no | removed |

- [ ] **Step 3: Write confirmation and cancellation tests**

`--clean` and `--delete-memory` refuse non-interactive deletion without `--yes`; interactive cancellation performs no destructive call and records `cancelled`.

- [ ] **Step 4: Implement default install/reinstall/uninstall**

Default uninstall removes only `venv/` after client cleanup. It does not describe or execute venv recreation. Reinstall never deletes durable directories.

- [ ] **Step 5: Implement full wipe and clean install**

Validate the root immediately before deletion, not only at operation start. Clean install recreates the root/layout after deletion and does not reuse deleted client records.

- [ ] **Step 6: Implement recovery paths**

If client restoration fails after successful runtime promotion, keep the runtime, fail the operation visibly, and print exact recovery instructions. If promotion fails, restore the previous runtime and prior registrations.

- [ ] **Step 7: Run operation and full suites**

Run: `python -m pytest tests/installer/test_operations.py -q && python -m pytest -q`

- [ ] **Step 8: Commit**

```bash
git add neo_localmcp/installer/operations.py neo_localmcp/installer/__init__.py tests/installer/test_operations.py
git commit -m "feat: orchestrate safe setup v2 lifecycle operations"
```

**Gate:** The semantic matrix is executable and default uninstall cannot reach any durable-root deletion function.

---

### Task 13: Add `setup_v2.py`, normalize installed commands, and run macOS acceptance

**Purpose:** Deliver the first usable front door and prove the full lifecycle on the currently available Mac.

**Model / effort:** GPT-5.5 High.

**Files:**
- Create: `setup_v2.py`
- Create: `tests/installer/test_setup_cli.py`
- Create: `tests/installer/test_macos_lifecycle.py`
- Modify: `neo_localmcp/cli.py`
- Modify: `neo_localmcp/tools.py`
- Modify: `README.md`

**Interfaces:**
- CLI: `python setup_v2.py install [--clean] [--yes] [--dry-run]`.
- CLI: `python setup_v2.py reinstall [--dry-run]`.
- CLI: `python setup_v2.py uninstall [--delete-memory] [--yes] [--dry-run]`.
- All parsers support `-h`/`--help` and return standard exit codes: 0 success/cancelled-without-error, 1 operation failure, 2 usage/safety refusal.

- [ ] **Step 1: Write parser/help tests**

Assert top-level and each subcommand expose `-h` and `--help`; incompatible flags fail before mutation; `--dry-run` prints the state and ordered action plan.

- [ ] **Step 2: Write Python-floor bootstrap tests**

Keep the top of `setup_v2.py` stdlib-only and emit a direct Python 3.12+ requirement before importing package modules.

- [ ] **Step 3: Implement the thin dispatcher**

The file parses arguments, constructs `OperationContext`, invokes one operation, renders the summary, and returns an exit code. It contains no path, migration, process, runtime, or deletion policy.

- [ ] **Step 4: Remove installed-command ambiguity**

Replace current registration-only `neo-localmcp setup` with explicit operational client configuration commands, preferably `neo-localmcp config clients setup|remove|status`. Keep `remove-client` only as a one-release deprecated compatibility alias if existing clients/scripts require it. No installed command performs installation lifecycle work.

- [ ] **Step 5: Run isolated CLI integration tests**

Run: `python -m pytest tests/installer/test_setup_cli.py -q`

Expected: dry runs mutate nothing; help exits 0; unsafe roots exit 2.

- [ ] **Step 6: Run real macOS lifecycle acceptance in a temporary home**

Test sequence:

```text
install -> seed config/SQLite/client record -> start MCP -> reinstall
-> verify PID exited and data survived -> uninstall -> verify venv absent/data present
-> install -> verify reuse message/data -> install --clean --yes
-> verify old data absent/new endpoint healthy -> uninstall --delete-memory --yes
-> verify root absent
```

Run: `python -m pytest tests/installer/test_macos_lifecycle.py -q -m slow`

- [ ] **Step 7: Run all local gates**

Run: `python -m pytest -q && python -m compileall -q neo_localmcp setup_v2.py`

- [ ] **Step 8: Commit**

```bash
git add setup_v2.py neo_localmcp/cli.py neo_localmcp/tools.py README.md tests/installer/test_setup_cli.py tests/installer/test_macos_lifecycle.py
git commit -m "feat: add cross-platform setup v2 entrypoint"
```

**Gate:** The Mac can complete the entire lifecycle without invoking `.ps1` or `.sh` files and without losing seeded memory.

---

### Task 14: Prove Windows and Claude Desktop lock-handling parity

**Purpose:** Validate the most important original failure mode before replacing the Windows installers.

**Model / effort:** GPT-5.5 High. Use a fresh implementation session with at least 40% context budget available.

**Files:**
- Create: `tests/installer/test_windows_lifecycle.py`
- Modify: `neo_localmcp/installer/processes.py` only if real Windows evidence requires it.
- Modify: `neo_localmcp/installer/runtime.py` only if real Windows replacement evidence requires it.
- Update: `SETUP_V2_DESIGN.md` only for confirmed platform nuances that do not change approved semantics.

**Environment strategy:** Windows x64 CI is the durable compatibility gate. A
temporary Windows 11 VM in Parallels is only a targeted live smoke-test appliance;
it is not a development architecture to optimize. Windows ARM may be used when it
is the convenient VM, but receives no architecture-specific code path unless a
real defect also affects supported users and cannot be solved in shared Python.
Install Python 3.12+, use a disposable checkout/home, and snapshot the VM before
destructive acceptance tests. Claude Desktop and Ollama are optional for the core
lifecycle run and required only for their targeted integration smoke tests.

- [ ] **Step 1: Run platform-neutral unit/full suites on Windows x64 CI**

Run: `py -3.12 -m pytest -q`

Expected: all non-platform-skipped tests pass.

- [ ] **Step 2: Run the real Windows lifecycle test in CI and once in the temporary VM**

Run: `py -3.12 -m pytest tests/installer/test_windows_lifecycle.py -q -m slow`

The test must start a managed MCP server, reinstall while live, confirm graceful exit/no file lock, then default-uninstall while live and confirm durable data remains.

- [ ] **Step 3: Run one targeted Claude Desktop extension-tree smoke test when the VM supports it**

Start the installed extension so the chain contains `uv.exe` and descendants. Run `setup_v2.py reinstall --dry-run` first to inspect owned targets, then perform reinstall. Confirm unrelated `uv.exe`, Python, Ollama, and console processes remain alive.

If Claude Desktop is unavailable or unreliable on Windows ARM, do not build an
ARM workaround. Run this smoke test on an x64 Windows environment later and keep
the old installers until that specific lock-handling gate has evidence.

- [ ] **Step 4: Test forced escalation explicitly**

Use a fixture helper that ignores graceful stop. Confirm only that verified test tree is terminated and the runtime becomes replaceable without restarting Windows or Claude Desktop.

- [ ] **Step 5: Test all destructive semantics**

Repeat the macOS acceptance matrix, including cancellation, `--yes`, clean install, default uninstall, full wipe, broken venv, and interrupted metadata recovery.

- [ ] **Step 6: Record evidence and fix only observed platform defects**

Add regression tests before each fix. Do not add a PowerShell policy layer; a small Python platform adapter is allowed only when `psutil`/stdlib cannot provide reliable behavior.

- [ ] **Step 7: Commit Windows parity**

```bash
git add tests/installer/test_windows_lifecycle.py neo_localmcp/installer SETUP_V2_DESIGN.md
git commit -m "test: prove setup v2 windows lifecycle parity"
```

**Gate:** Reinstall and uninstall work with live Claude Desktop/managed processes and require no application or OS restart.

---

### Task 15: Add Linux acceptance and automated three-OS regression gates

**Purpose:** Verify the shared lifecycle is genuinely portable rather than merely Mac/Windows compatible.

**Model / effort:** GPT-5.5 Medium for workflow setup; switch to High only for platform failures.

**Files:**
- Create: `tests/installer/test_linux_lifecycle.py`
- Create: `.github/workflows/setup-v2.yml` if GitHub Actions is the chosen repository CI.
- Modify: `pyproject.toml` to register `slow` and platform markers if not already configured.

**Interfaces:**
- CI matrix: `macos-latest`, `windows-latest`, `ubuntu-latest`, Python 3.12.
- Fast job runs full unit suite; lifecycle job runs isolated real install/reinstall/uninstall tests without touching the runner’s actual home.

- [ ] **Step 1: Add Linux real-lifecycle test using the shared acceptance sequence**

The test uses a temporary `NEO_LOCALMCP_HOME`, starts a real MCP process, and validates memory survival/default uninstall/full wipe.

- [ ] **Step 2: Add the CI matrix**

Install the local checkout with test dependencies, run `python -m pytest -q`, run platform lifecycle tests, then run `python -m compileall -q neo_localmcp setup_v2.py`.

- [ ] **Step 3: Keep network-dependent Ollama checks mocked in CI**

Real Ollama unload behavior remains a manual smoke test; CI verifies request shape, timeouts, and failure degradation without downloading models.

- [ ] **Step 4: Run workflow-equivalent commands locally on macOS**

Run: `python -m pytest -q && python -m pytest tests/installer/test_macos_lifecycle.py -q -m slow && python -m compileall -q neo_localmcp setup_v2.py`

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/setup-v2.yml pyproject.toml tests/installer/test_linux_lifecycle.py
git commit -m "ci: verify setup v2 across windows macos and linux"
```

**Gate:** All three operating-system jobs pass from a clean checkout.

---

### Task 16: Rename the entrypoint, retire old installers, document 1.0.10, and release

**Purpose:** Make the proven Python lifecycle authoritative and remove obsolete policy duplication.

**Model / effort:** GPT-5.5 High for retirement audit, then Medium for documentation/version synchronization.

**Files:**
- Rename: `setup_v2.py` to `setup.py`
- Delete: `setup.ps1`, `install.ps1`, `uninstall.ps1`, `install.sh`, `uninstall.sh`
- Delete or rewrite: `tests/test_upgrade_cycle.py` so retained migration fixtures no longer execute deleted scripts.
- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `docs/ARCHITECTURE.md`
- Modify: `PROJECT_STATUS.md`
- Append: `PROJECT_NOTES.md`
- Modify: `neo_localmcp/__init__.py`
- Modify: `pyproject.toml`
- Modify: `packages/claude-desktop/mcpb/manifest.json`
- Modify: installer tests and documentation references from `setup_v2.py` to `setup.py`.

**Interfaces:**
- Final lifecycle interface is `python setup.py install|reinstall|uninstall`.
- Product version is synchronized to `1.0.10` in all three required version files.

- [ ] **Step 1: Search for every old installer/path/terminology reference**

Run:

```bash
rg -n "setup_v2|setup\.ps1|install\.ps1|uninstall\.ps1|install\.sh|uninstall\.sh|\.venv-nlm|current-venv|repo-context\.sqlite|config\.yaml" . -g '!*.mcpb' -g '!SETUP_V2_DESIGN.md' -g '!docs/1.0.10_PLAN.md'
```

Classify each match as migration compatibility, current behavior, historical documentation, or stale reference.

- [ ] **Step 2: Rename the entrypoint and update tests first**

Run focused CLI and lifecycle tests after the rename. No compatibility wrapper named `setup_v2.py` remains because the rename is literal.

- [ ] **Step 3: Remove old installer files and convert legacy integration coverage**

Keep fixture directories/data builders that prove old-layout migration, but route operations through `setup.py` instead of PowerShell/shell scripts.

- [ ] **Step 4: Rewrite user documentation around exact semantics**

Document preserved directories, clean install, full wipe confirmation, Python 3.12 prerequisite, local checkout source, client reconnection, Ollama model unloading, and three-OS verification. Remove instructions for installed `neo-localmcp setup` lifecycle behavior.

- [ ] **Step 5: Synchronize version 1.0.10**

Update `neo_localmcp/__init__.py`, `pyproject.toml`, and `packages/claude-desktop/mcpb/manifest.json` together. Rebuild the MCPB and run its byte-for-byte distribution tests.

- [ ] **Step 6: Update project truth files**

Move macOS/Linux installer parity out of Known limitations only after their gates pass. Add verified setup-v2 behavior to `PROJECT_STATUS.md` and a dated concise decision/evidence entry to `PROJECT_NOTES.md`.

- [ ] **Step 7: Run final automated verification**

Run:

```bash
python -m pytest -q
python -m compileall -q neo_localmcp setup.py
python setup.py --help
python setup.py install --help
python setup.py reinstall --help
python setup.py uninstall --help
```

Expected: all pass/exit 0.

- [ ] **Step 8: Run final live smoke sequence on macOS and Windows**

Use disposable homes first, then deliberately upgrade the real managed install only after disposable tests pass. Confirm preserved retrieval rows and client reconnection.

- [ ] **Step 9: Run stale-reference audit**

Repeat the Step 1 `rg`. Remaining old-layout references must exist only in migration code/tests or clearly labeled historical notes.

- [ ] **Step 10: Commit release closeout**

```bash
git add -A
git commit -m "release: replace platform installers with setup v2"
```

**Gate:** `setup.py` is the sole lifecycle policy surface, all three OS gates pass, and the old scripts are absent.

---

## Model and effort schedule

| Task | Deliverable | Recommended model | Effort | Can execute on Mac now? |
|---:|---|---|---|---|
| 1 | Package/types/test harness | GPT-5.5 | Medium | Yes |
| 2 | Safe path model | GPT-5.5 | High | Yes |
| 3 | Canonical application paths | GPT-5.5 | High | Yes |
| 4 | State + metadata | GPT-5.5 | High | Yes |
| 5 | Legacy migration | GPT-5.5 | High | Yes |
| 6 | Output + confirmations | GPT-5.5 | Medium | Yes |
| 7 | Owned process shutdown | GPT-5.5 | High | Yes, with fake graphs + Mac live test |
| 8 | Ollama model unload | GPT-5.5 | Medium | Yes |
| 9 | Client registration lifecycle | GPT-5.5 | High | Yes |
| 10 | Transactional runtime | GPT-5.5 | High | Yes |
| 11 | Endpoint verification | GPT-5.5 | High | Yes |
| 12 | Operation orchestration | GPT-5.5 | High | Yes |
| 13 | CLI + Mac acceptance | GPT-5.5 | High | Yes |
| 14 | Windows x64 CI parity + one disposable VM smoke test | GPT-5.5 | High | CI can start earlier; live lock test waits for any convenient Windows VM |
| 15 | Linux + three-OS CI | GPT-5.5 | Medium initially | Yes after Task 14 or in CI |
| 16 | Retirement + release | GPT-5.5 | High then Medium | Only after 14-15 pass |

### Guidance for the current GPT-5.5 Medium session

With roughly 60% usage remaining, use this session for the plan and, if desired,
Task 1 only. Start implementation in a fresh isolated worktree/session. Tasks 2-5,
7, and 9-14 should run at High effort because mistakes can lose data, kill unrelated
processes, corrupt external client configuration, or leave an unrecoverable runtime.
Medium is appropriate for bounded types, output formatting, Ollama request wrapping,
and CI/docs once the high-risk contracts already exist.

## Commit and review cadence

- One commit per task; do not combine destructive orchestration with its primitives.
- Pause for review after Tasks 3, 5, 7, 10, 12, 13, and 14.
- Do not edit/remove old installers before Task 16.
- Do not test destructive behavior against the real `~/.neo-localmcp` until the same
  operation passes against an isolated temporary home.
- For any unexpected failure, use `superpowers:systematic-debugging` before changing
  the design or adding platform-specific behavior.

## Final acceptance checklist

- [ ] Python 3.11 and older fail early with an actionable Python 3.12+ message.
- [ ] Install from a clean checkout creates exactly one managed `venv/`.
- [ ] Default install detects and reuses preserved memory/data.
- [ ] Reinstall replaces only `venv/` and reconnects recorded clients.
- [ ] Default uninstall removes `venv/`, unregisters clients, and recreates nothing.
- [ ] `install --clean` requires confirmation, wipes the root, then installs fresh.
- [ ] `uninstall --delete-memory --yes` wipes the root and does not reinstall.
- [ ] Legacy Windows and Unix layouts migrate without losing SQLite rows or config.
- [ ] Broken and interrupted runtimes are detected and recoverable.
- [ ] Neo-used Ollama models unload; the shared Ollama daemon remains running.
- [ ] Unrelated Python, uv, Ollama, and console processes survive every operation.
- [ ] A live MCP server releases its runtime without requiring an app/OS restart.
- [ ] CLI, package version, MCP handshake, paths, clients, and doctor are verified.
- [ ] Help, prompts, logs, tests, and docs use the approved terminology exactly.
- [ ] macOS, Windows, and Linux lifecycle suites pass.
- [ ] Old `.ps1`/`.sh` lifecycle implementations are removed only after parity.
