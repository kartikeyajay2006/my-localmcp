# Project Status

Updated: 2026-07-21

## Current phase

**`v1.2.0` is tagged and released** (`87b02f1`, PR #98, 2026-07-11) — surfaced index freshness + optional semantic ranking. `v1.1.1` (`60093d8`) and `v1.0.10` are also tagged; `v1.2.0` is the first new tag since `v1.0.10`. Issue `#65` (`--live-agent-comparison`) is closed, parked indefinitely.

**1.2.1: six issues implemented, fixed, and verified end-to-end on `dev/v1.2.1_implementation`; `main` untouched, only the Claude Desktop `.mcpb` GUI install remains manual.** The original four — `#100` (PR #105), `#103` (PR #106), `#68` (PR #107), `#101` (PR #108) — landed first with automated macOS+Windows CI green on every sub-PR. The owner's own manual OS testing pass against this branch then surfaced 9 further findings (`#109`-`#117`, logged in `docs/1.2.1_MANUAL_OS_TESTING.md`); two client-registration reliability bugs from that batch — `#112`/`#114`, plus `#113` — are folded in as phase 12.1h (2026-07-21). `#112` turned out to have **three** stacked root causes, the second and third found only by driving real (non-mocked) end-to-end lifecycle runs rather than testing install in isolation: (1) `#114`, the wizard's missing `client_selection_explicit` flag; (2) a shared bug in `_install_like()`'s client-intent pre-write silently defeating `apply_client_selection`'s diff, so a real explicit-selection install (wizard *or* CLI) could connect zero clients while reporting success — likely present in production since #103 merged; (3) found only when explicitly asked "are you sure reinstall and uninstall->install work the same way" and testing the full lifecycle for real: a default uninstall deliberately keeps the client's `registrations.json` record (so a later reinstall can restore it) while removing the live registration, and `apply_client_selection`'s diff treated that stale-but-present record as "already connected," so a subsequent install/reinstall never reconnected it. All three are now verified live (sandboxed `$HOME`, real venv builds, real `claude mcp add`/Codex `config.toml` writes, never touching this machine's real managed root) — including a full install -> reinstall -> uninstall -> install sequence, all four steps now succeeding — against both requested environments (localhost Ollama on the MacBook Pro, and Ollama pointed at NeoRyzen; NeoRyzen's Tailscale host is reachable but Ollama itself wasn't listening there at test time, so only the never-blocks config-write path was exercised for that half). See `docs/1.2.1_PLAN.md`'s 12.1h entry and `PROJECT_NOTES.md` 2026-07-21 entries for full mechanism and evidence. The remaining 7 findings (`#109`-`#111`, `#115`-`#117`) are UX/transparency gaps, explicitly deferred to a follow-up pass by the owner, not release-blocking. Remaining before 12.1g (final integration): only the real Claude Desktop `.mcpb` GUI install (Settings > Extensions), reserved for the owner since it can't be driven headlessly; then the version bump, `.mcpb` rebuild, Opus final-integration audit, and the single `dev -> main` PR + tag. See `docs/1.2.1_PLAN.md`'s "Implementation progress" section for full per-issue detail.

**1.2.1 scope finalized (2026-07-12): four issues, severity-ordered, one integration branch.** `docs/1.2.1_PLAN.md` now bundles, in priority order: `#100` (wizard/CLI install never puts `neo-localmcp` on `PATH` — breaks the CLI universally post-install, highest severity), `#103` (client selection is inconsistent across install/reinstall/uninstall, and an explicit Install-time client pick can be silently discarded by `_record_client_intent`), `#68` (`OLLAMA_MODELS` not explicitly passed to the spawned `ollama serve` process under some launch ancestries — narrowest blast radius, local-Ollama-only, diagnosable symptom), and `#101` (general/hardware-blind per-role model recommendations in the wizard, split out of `#97` during 1.2.0 dogfooding — a feature, run last). Each gets its own branch/PR into a `dev/v1.2.1_implementation` integration branch (with manual macOS+Windows verification per issue) before a single PR lands on `main`, since this repo's CI only triggers on pushes to `main` or on PRs — a bare feature-branch push gets no CI signal otherwise. The harder **hardware-aware** recommendation version (real VRAM/RAM detection across NVIDIA/Apple Silicon/AMD/Intel) stays tracked standalone in issue `#97` and layers on afterward without changing the UI again. "Brand consistency" stays explicitly deferred, no version assigned. What was `docs/1.2.1_PLAN.md` (onboarding distribution — `bootstrap-repo`) is renamed to `docs/1.2.2_PLAN.md` to make room, same precedent as the `1.2.0_PLAN.md` → `1.1.1_PLAN.md` rename earlier in this project's history.
- **1.2.0 phase 12a complete (2026-07-10): `prepare_context` surfaces index freshness inline.** `index_repo` stamps an `indexed_commit` repo-metadata key at index completion; `prepare_context` compares it to current HEAD and prints one line — "index: fresh, at HEAD" or "index: N commits behind HEAD; run refresh_index for current results" — with the structured `index_freshness` dict in the JSON payload. Pre-12a indexes (no `indexed_commit`), non-git repos, and unresolvable old commits degrade to the previous output (no line, no error); the key backfills on the next index/refresh. Deterministic, no Ollama involvement. TDD, 5 regression tests in `tests/test_context.py`.
- **1.2.0 phase 12b complete (2026-07-10): optional semantic-rerank layer.** New `ollama.embed_model` config key (default `None` = disabled; settable via `set-ollama --embed-model`). New non-blocking `ollama_client.embed()` and a `file_embeddings` table (float32 vectors, keyed per-path on content hash, regenerated on source change, cleaned up on delete/reset). Embeddings generate lazily in `index_repo` for changed paths only, when `embed_model` is set; a down/busy/embeddings-disabled Ollama is skipped (one probe, never blocks or errors indexing). `mcp/memory.py` re-ranks only the already-FTS-selected candidates by `cosine * 18`, gated on `repo_has_embeddings()` so an unset repo is byte-identical to pre-12b (no `embed()` call). **Never-blocks validated live in an isolated home** (Ollama at a dead port: `prepare_context` 0.27s, `index_repo(force)` 0.08s on FTS alone; embeddings-disabled server → graceful skip). TDD, 15 tests in `tests/test_semantic_ranking.py` + migration test extended.
- **12b acceptance benchmark: baseline vs. configured delta captured (2026-07-10), against a real GPU-backed Ollama.** This machine's local Ollama has no embeddings support and no embed model; owner's second machine (`neofedora`, RTX 3060, Fedora, reachable over Tailscale) already had `mxbai-embed-large` and `bge-m3` installed. Timed both on a realistic ~20k-char file body: `mxbai-embed-large` averaged ~0.4s/call once warm (1024-dim, 669MB) vs. `bge-m3`'s ~2.2-2.6s/call; picked `mxbai-embed-large`. This repo's real config now points `base_url` at neofedora with `embed_model=mxbai-embed-large` (owner's explicit choice, kept permanently rather than reverted to localhost after the benchmark) — `fast_model`/`summary_model` were reset to real values (`qwen3:8b` / `qwen3-coder:30b`) after finding stale placeholder values (`new-fast`/`new-summary`) already in the config, unrelated to this session. A forced reindex generated 166 real embeddings against neofedora in ~88s. Re-ran `benchmarker`'s `mem` group configured: still 16/16 pass, still fully deterministic, median discovery/read token-reduction rose **0.4304 -> 0.4837** (baseline unset -> configured). Per-query comparison: **every query with a known `gold_file` kept the identical rank** between baseline and configured runs -- the semantic layer never overrode structural FTS ranking on this benchmark set, exactly matching the "additive, near-tie-only nudge, below any structural signal" design intent. The token-reduction gain traces to 3 of 16 checks reordering *within* already-near-tied candidates, shifting which excerpts got pulled inside the token budget -- a real, modest, structurally-safe effect, not a ranking override. Acceptance evidence for 12b is now complete.
- **`embed_model` now exposed in the setup wizard and `setup.py config-ollama` (2026-07-11), closing the follow-up flagged during 12b.** The wizard's Ollama step gained an optional embedding-model phase (`_phase_ollama_embed`) that lists installed models with a "0) None" disable option and describes the semantic layer's purpose; `OllamaInfo`/`WizardState` carry `embed_model`, and both wizard backends (live + preview) thread it through. `installer/configure_models` is now tri-state on `embed_model` (`None` keeps current, `""` explicitly disables, a name enables) so the wizard's "None" choice and `set-ollama --embed-model ""` / `config-ollama --embed-model ""` can actually turn it off. README updated. TDD: tri-state config test, live/preview backend threading tests, and a scripted console-phase smoke.
- **Two wizard bugs fixed (2026-07-11), found while dogfooding the above.** (1) The Ollama endpoint page's free-text prompt now shows the current value, asks "Looks good?" (default Yes), and only on No loops through validated re-entry (`scheme://host[:port]`) until accepted. (2) Model-picker pages now render an aligned table with real `size` + `kind` ("chat"/"embed") columns — `kind` comes from Ollama's own `/api/tags` `capabilities` tag, not a guess, at zero extra network cost (new `ollama_client.model_details()` sibling to `model_sizes()`, same single call). The current selection's row is colored. TDD throughout; full suite green, `.mcpb` rebuilt.
- **1.2.0 phase 12c complete (2026-07-10): semantic matching for retrieval-boost memory.** `record_task_query` now stores a lazy, optional embedding of the raw query text (`task_queries.embed_model`/`query_vector`, same non-blocking rule as 12b: `embed_model` unset -> zero network, a failed embed leaves the columns `NULL`, the row itself is never blocked). `get_boost_map`'s exact-`term_key` lookup stays the unchanged, zero-cost first path (refactored into `_boost_rows_for_term_key`, byte-identical SQL/logic, reused by both paths); only when that returns nothing does a new `_semantic_boost_fallback` fire — it embeds the current task, scans this repo's other stored task-query embeddings for the closest paraphrase (cosine >= `SEMANTIC_BOOST_THRESHOLD=0.82`), and applies a down-weighted fraction (`SEMANTIC_BOOST_DOWNWEIGHT=0.5`) of *that* term_key's own already min-shown/cap-gated boost — never full credit, never overriding a real exact match. `_cosine` moved from `mcp/memory.py` into `repo_memory.py` (now needed in both; `mcp/memory.py` imports it, matching the repo's dependency direction) and `_apply_retrieval_boost` now threads the raw task string through to `get_boost_map`. TDD, 8 new tests appended to `tests/test_semantic_ranking.py` (exact-match zero-cost-first-path, paraphrase match, unrelated-task rejection, embed_model-unset regression, pre-12c-signature regression, end-to-end `prepare_context` integration) + migration test extended for the 2 new `task_queries` columns.
- **Test-isolation bug fixed (2026-07-11), found while re-running the suite for 12c.** `tests/conftest.py`'s `isolated_app_home` fixture only set `NEO_LOCALMCP_HOME`, but `config.py`'s `APP_DIR`/`CONFIG_PATH` are computed once at module import time and aren't re-read live the way `NEO_LOCALMCP_CONFIG` is — so tests using it (notably one hardcoding `fast_model="new-fast"`/`summary_model="new-summary"`) silently wrote to the developer's real `~/.neo-localmcp/config/config.yaml`, not a tmp sandbox. Fixed by also monkeypatching `config.APP_DIR`/`CONFIG_PATH` directly, mirroring the already-correct sibling fixture. Regression test added; full suite green.
- **`gitleaks` pre-commit hook added (2026-07-11)** after a real leak (a developer's personal Tailscale hostname landed in a `PROJECT_NOTES.md` commit, caught and fixed via history rewrite before merge). `.gitleaks.toml` layers project rules — Tailscale MagicDNS hostnames, Tailscale CGNAT IPs (`100.64.0.0/10`), and general public IPv4 addresses (with loopback/RFC1918/link-local/multicast/reserved ranges allowlisted so routine `127.0.0.1`/`192.168.x.x`/version strings like `0.4.2.5` don't false-positive) — onto gitleaks' default secret-detection ruleset. Hook lives in-repo (`.githooks/pre-commit`), portable by construction; activation is a one-time `git config core.hooksPath .githooks` per clone (documented in `.github/CONTRIBUTING.md`). No new language dependency — just the `gitleaks` binary.
- **1.2.0 phase 12d (release) in progress** — see "Current phase" above for full detail.
- **Installer/mcp-command reorg complete (2026-07-07)**: the installer's two frontends (the scriptable CLI, the interactive wizard) are consolidated under `neo_localmcp/installer/` (`installer/cli.py`, `installer/wizard/`); `tools.py`'s single-file MCP tool surface is split into `mcp_commands/{system,memory,ollama,editing}.py` + `_shared.py` by category; `benchmark.py`/`benchmark_queries/` are renamed to `benchmarker/`; and wizard "dummy"/"fake" terminology is standardized to "preview" throughout. No version bump — folds into the still-unreleased 1.1.1. `.mcpb` bundle rebuilt against the new layout and `CLAUDE.md`'s module map updated to match; full suite (including the real lifecycle/slow tests and the bundle byte-check) green.
- **Closed #64 (2026-07-06)**: `lookup()` searches the stored index, not live files, and deliberately skips freshness probing on its hot read-only path (a considered perf tradeoff, not an oversight — see the issue). Of the three directions discussed in the issue, went with the cheapest: `index_repo` now stamps a repo-level `last_indexed_at` metadata key (alongside the existing `indexer_version`/`eligible_files`/etc. writes) every time it completes, and `lookup()` surfaces it via one single-key `repo_metadata` read — no per-file rehash, no git/filesystem probing, so the "millisecond query becomes a client timeout" risk the issue's own code comment warns about is untouched. Lets a caller judge staleness risk itself. TDD: `tests/test_repo_memory.py::test_lookup_reports_last_indexed_at_from_stored_metadata` and `::test_lookup_reports_no_last_indexed_at_for_a_never_indexed_repo`, both watched red before the implementation.
- **Flat root-module rename (2026-07-09)**: the remaining flat `neo_localmcp/` modules were renamed for clarity, since the reorg (above) left them as the last vaguely-named files: `identity.py`→`branding.py`, `lifecycle.py`→`mcp_server_lifecycle.py` (was colliding with `installer/`'s own "lifecycle" framing), `utils.py`→`repo_utils.py`, `client_setup.py`→`ai_client_config.py`, `cli.py`→`runtime_cli.py` (to read unambiguously next to `installer/cli.py`). `config.py` and `ollama_client.py` were reviewed and kept as-is. All call sites updated (bare-module imports use an alias, e.g. `from .. import mcp_server_lifecycle as lifecycle`, to avoid a large mechanical diff at every usage site); `pyproject.toml`'s `neo-localmcp` console-script entry point updated to `neo_localmcp.runtime_cli:main`; `CLAUDE.md`'s module map and `.github/CONTRIBUTING.md`'s area labels updated to match; `.mcpb` bundle rebuilt. Full suite green (351 passed / 1 skipped / 6 deselected), compileall clean.

1.0.10 setup lifecycle work (Phases 1-16) is complete. `setup.py` is the sole supported lifecycle entrypoint on macOS and Windows; Linux is explicitly deferred. Native Python 3.12 CI is green on macOS and Windows (run 28632598884), and the real managed installation is running 1.0.10 from a clean canonical layout.

`setup.py` installs, reinstalls, default-uninstalls while preserving durable data, clean-installs, and full-wipes from a local checkout. It builds and verifies a candidate runtime before promotion, stops Neo-owned processes, unloads only Neo-used Ollama models, migrates recognized legacy layouts, reconnects explicitly selected/recorded clients, and verifies the installed CLI/MCP endpoint.

1.0.9 onboarding-polish work (`docs/1.0.9_PLAN.md`) is complete and released.

## Acceptance targets

- At least 50% fewer discovery/read tokens on representative coding tasks.
- At least 30% fewer total task tokens without lower edit or test accuracy.
- Complete indexes explicitly report completeness and prune vanished files safely.
- Ollama failures never suppress deterministic repository context.
- Normal Ollama operations have bounded health, start, warm, and inference deadlines.

## Verified

- **`setup.py` now exposes all five wizard operations (2026-07-06): install, reinstall, uninstall, config-ollama, manage-clients.** The underlying Ollama-config-writing logic (`installer/ollama.py::configure_models`) and client-reconciliation logic (`installer/clients.py::apply_client_selection`) are now shared across three callers (the interactive `setup_wizard.py`, the CLI `setup.py`, and the runtime `neo-localmcp set-ollama` command), deduping the write path to one implementation each. CLI/wizard option parity is now complete, discovered while tracing the full setup lifecycle call chain.
- **The #10 audit backlog is now fully closed (2026-07-06): #26, #36, #30, #13, #29** (5 PRs: #58-#62), worked in ascending risk order. Windows CLI mojibake fixed (3 non-ASCII glyphs -> ASCII in `tools.py`'s text renderers). Wizard's 3 private cross-module reaches promoted to public APIs (`ollama_client.model_sizes()`, `setup_cli.dry_run_plan()`). Result-envelope duplication removed across `server.py` (`@_tool_guard`), `ollama_client.py` (`_result()`/`_elapsed()`), and `tools.py` (`_project_read_first_item()`/`_git_summary()`). CI split into named per-area jobs (installer/retrieval/ollama/other x 2 OS) with a structural completeness test; `neo_localmcp/wizard/` went from zero pytest coverage to 8 tests. `context_prepare` decomposed from 233 lines/8 inline jobs to 102 lines/7 named helpers, verified byte-identical via a golden `test-determinism` hash baseline re-checked after every single extraction against a frozen git-worktree snapshot (verifying against the live self-indexing repo while editing it was found to shift the retrieval target itself -- a methodology bug, not a code bug). #48 (selective test execution) deliberately left alone despite being technically unblocked -- its own issue text says "do NOT build yet," an explicit owner deferral. Full suite green after the whole batch: 316 passed / 4 skipped, compileall clean. Remaining open: #48 (deferred), #9 (not urgent).
- **Four more low-risk items from the #10 audit closed (2026-07-05): #31, #32, #33, #35**, split into 3 PRs by area (#54 mcp-mgmt, #55 retrieval, #56 client-integration) per the owner's Issue-to-PR preference. `config.py`'s `config.yaml`-is-actually-JSON drift is now documented at the point `CONFIG_PATH` is defined (no code change; a rename or real YAML parser were both considered and rejected as disproportionate). `doctor`'s hand-maintained, drift-prone `"commands"` array was dropped in favor of pointing at `--help`. `query.py`'s `FILLER_WORDS` is now derived from `INTENT_KEYWORDS` instead of hand-synced. `client_setup.py`'s `remove_codex` no longer re-reads the file 3x and its `ok` boolean is no longer triple-negated; `_detect_registered_scope`/`_migrate_claude_code_registration` were extracted to dedupe `setup_claude_code`/`remove_claude_code`. All behavior-preserving except #31/#32 (no behavior to preserve). Full suite green after the batch (305 passed / 4 skipped), compileall clean.
- **Three retrieval-quality bugs from the #10 audit's dogfooding sessions are fixed and regression-tested (2026-07-05): #22, #23, #24.** `lookup` now indexes actual file content (not just path+language), so string-literal/SQL identifiers that never appear as a `def`/`class` symbol are findable the same way a plain grep finds them. `repo_fts`'s `repo_id`/`kind`/`target` columns are now `UNINDEXED` and per-symbol bodies no longer duplicate the file path, so an overloaded word that's merely a filename substring (e.g. "migration") no longer matches every symbol in that file; a new distinct-strong-term-coverage bonus in `tools.py` ensures a file matching several different strong terms outranks one with many repeated hits on a single weak term. `query.py`'s `_is_symbol_like` now recognizes snake_case identifiers (e.g. `summary_model`) as strong terms instead of demoting them to weak. `INDEXER_VERSION` bumped to `1.2.0` so existing repos rebuild automatically. Full suite green (304 passed / 4 skipped), compileall clean, `.mcpb` rebuilt.
- **Guided terminal installer (`setup_wizard.py`), 2026-07-03, merged to `main` as part of v1.1.0**: a plain, stdlib-only, full-screen *numbered* wizard over the existing `setup.py` lifecycle (an initial Textual version was replaced at owner request — see `PROJECT_NOTES.md` 2026-07-03 (8); it survives at commit `0c31fcb`). Clears the screen, builds a running summary as you answer numbered questions, with per-client path/detail persisted in that summary once chosen. Menu adapts to detected state (first-time install vs. returning-user reinstall / Ollama config / manage clients / uninstall); Ollama models are picked from the live `ollama list`, alphabetically sorted with sizes; every prompt supports "b"/"back" via a small phase machine and shows its default as `[Default: <val>]`; only destructive path is full-wipe-under-uninstall behind the existing typed-`DELETE` gate. Clean separation via a `WizardBackend` Protocol with fake (`--fake`, side-effect-free) and real backends; the real backend only *calls* `installer/` + `config`/`ollama_client`/`client_setup`, adding no lifecycle policy. No third-party UI dependency — the `[wizard]` extra only pins `psutil` (already a base dep).
  - **Confirmed working on macOS by the owner** (2026-07-03): ran `--fake` end-to-end, paths correctly showed mac-style `/Users/...` formatting.
  - **Real (non-`--fake`) lifecycle verified end-to-end and in isolation on Windows** (2026-07-03, see `PROJECT_NOTES.md` (13) for the full method): install / reinstall / uninstall (both runtime-only and full-wipe) driven for real via subprocess against a disposable `NEO_LOCALMCP_HOME`, never touching the real managed root -- real venv build, real MCP handshake, real `doctor` check all passed, durable-data preservation on runtime-only uninstall confirmed, and a real bug (`_save_prefs` silently recreating `config/` after a full wipe) was found and fixed as a direct result of testing the real path instead of trusting the code read.
  - Remaining before this can be treated as fully done: an interactive human run in a real terminal (all testing so far is piped-input or owner's manual `--fake` run), and a real (non-`--fake`) run on macOS specifically.
- **Live end-to-end MCP stress test against a real Windows install, 2026-07-03**: all 12 MCP tools (`doctor`, `ollama_status`, `ollama_ensure`, `prepare_context`/`context_prepare` alias, `file_excerpts`, `repo_status`, `refresh_index`, `repo_lookup`, `summarize_file`, `apply_patch` check-only, `record_change`) exercised live against this repo's own real `~/.neo-localmcp` install (3 concurrently running managed servers: 2 Claude Desktop extension hosts + 1 CLI-launched, all Windows-verified alive/reporting correctly). Started from genuinely zero memory for this repo (0 indexed files) and built up through real usage to 104 indexed files / 1094 symbols / 7 recorded task queries / 1 change event. Retrieval-boost memory was watched live crossing its `min_shown=3` threshold on a real repeated query (`neo_localmcp/ollama_client.py` score rose 443 -> 445 after 3 shown + 3 followed rounds), and Ollama's deterministic-fallback guarantee held (`summarize_file` returned full deterministic context with `ollama_summary.ok=false` cleanly, never blocking). Token-budget accounting was accurate to within ~1.5% of requested budget (`estimated_tokens=406` for `token_budget=400`, `601` for `600`), and a bounded single-query response (~1000-1200 estimated tokens covering 5 ranked candidates) was roughly 8-10x cheaper than reading the same 5 whole files directly (52,480 raw chars / ~13,120 tokens), consistent with the >=50%/30% acceptance targets. Found and fixed/reported two new gaps in the process (see Known limitations): `exclude_dirs` exact-name-only matching let a leftover `.venv-phase14` test venv 100%-pollute ranking for an affected query, and retrieval-boost "followed" credit only matches the primary shown excerpt window, not every listed hint line. `codex/settings-rework` was fast-forwarded to include all 1.0.10 work and opened as PR #2 into `master`.
- **Native 1.0.10 release gate is green on macOS and Windows with Python 3.12**: GitHub Actions run 28632598884 passed fast tests, each platform's real lifecycle, and compileall. macOS completed in 1m32s; Windows completed in 14m55s.

- **Setup v2 Phase 14 is verified on Windows x64**: promoted `python.exe`, `pip.exe`, `neo-localmcp.exe`, and `neo-localmcp-server.exe` remain runnable after staging relocation; a real disposable lifecycle passed live MCP reinstall/uninstall, durable-data reuse, clean/full wipe, cancellation refusal, broken/interrupted recovery, and unrelated-process survival. Ownership-scoped escalation killed only the registered test tree. Final suite: 281 passed, 4 platform/environment skips; compileall clean (Python 3.14.5, the available supported interpreter).

- **Setup v2 through Phase 13 is verified on macOS in an isolated managed home**: the real lifecycle test completes install -> seed config/SQLite/client record -> start MCP -> live reinstall -> default uninstall with data preserved -> install reusing preserved data -> clean install -> full wipe. Real candidate promotion and the installed MCP handshake/doctor verification also pass.
- **Client-removal success reporting is fail-safe**: automated registration removal now returns a machine-readable result, and setup v2 aborts before removing the runtime or managed root if cleanup fails. Claude Desktop's intentionally manual removal remains a visible warning rather than a false automated-success claim.

- Automated regression suite passes on Windows (82 tests), including real MCP stdio handler calls, helper-process protocol isolation, byte-for-byte verification that every packaged `neo_localmcp` source/template matches the working tree, Markdown heading-section retrieval, retrieval-memory behavior, Ollama enrichment bounding, the graceful-stop lifecycle, and the Windows upgrade cycle end-to-end including the 1.0.8 single-venv-per-version behavior (same-version reinstall is a no-op, `-Repair` forces a real rebuild, a different version gracefully stops and replaces the old venv).
- 1.0.8 built and installed: `packages/claude-desktop/neo-localmcp.mcpb` rebuilt and verified byte-identical to source; the CLI install (`~/.neo-localmcp`) was upgraded from a real leftover 1.0.7 install to 1.0.8. The separately-installed Claude Desktop/Code extension was deliberately left untouched — swap the rebuilt `.mcpb` in manually when ready.
- **Retrieval-boost memory (P5) is now audited, upgrade-safe, and tunable (1.0.9, 9g)**: a 2026-07-01 live multi-call session against this repo confirmed the wiring works as designed -- `shown`/`followed`/`corrected` counts move on real `prepare_context`+`file_excerpts` calls, a boost only appears once the same task has been shown >= `min_shown` (3) times, grows with net-followed, and stays capped (8) far below any structural signal (a heading milestone match is +60); silence (shown-but-not-followed) is never penalized. Upgrade persistence is now asserted by a test: `retrieval_boost` rows survive a real venv-swap install intact (the guarantee is data-level -- the db file bytes do legitimately change because `init`/`doctor` write to it, but the memory data does not). `RETRIEVAL_BOOST_CAP`/`RETRIEVAL_BOOST_MIN_SHOWN` are promoted to config (`memory.retrieval_boost_cap`/`retrieval_boost_min_shown`), defaults unchanged since the audit found no evidence to move them (kept at 3 by explicit decision).
- Deterministic context returns current hashed excerpts within a requested budget.
- Live Ollama status distinguishes installed and loaded models at the configured endpoint.
- Live supervisor warm-up loaded `qwen3:8b` fully into VRAM in about 14 seconds; bounded ranking inference completed in about 11 seconds and end-to-end context plus Ollama advisory completed in about 19 seconds.
- Codex app/CLI/IDE configuration is unified at `~/.codex/config.toml`.
- **Windows upgrades are now genuinely graceful, verified live, not just in principle**: a running server responds to `neo-localmcp stop` by exiting itself within seconds, releasing its file locks cleanly.
- **Windows installs now keep exactly one venv per version, not one per install run** (1.0.8): 1.0.7's side-by-side scheme was a workaround for the pre-graceful-stop era and, left in place, caused real repeated-pip-install disk churn (traced live to spiking C: drive utilization across a heavy testing session, unrelated to Ollama model storage on a separate drive). Reinstalling the same version is now a ~1.2-second no-op instead of a 40-90 second rebuild, verified live; upgrading to a new version sweeps the entire legacy `venvs/` directory in one pass. macOS (`install.sh`) is still on the old side-by-side scheme, same 7d gap as before, tracked in `docs/1.0.7_PLAN.md`.
- **1.0.9 phases 9a-9f verified**: `install.ps1` reports Ollama presence and per-model (`fast_model`/`summary_model`) availability at install time without ever blocking -- every external Ollama call is wrapped in a time-bounded (10-15s) PowerShell job after a real hang was caught by `tests/test_upgrade_cycle.py` and fixed. `.mcpb` packaging is versioned (`packages/claude-desktop/neo-localmcp-v1.0.8.mcpb`, rebuilt and verified byte-identical to source), with a fixed-name local copy still placed at `~/.neo-localmcp/neo-localmcp.mcpb` for Claude Desktop's manual-install instructions. `setup.ps1`'s client registration step is now per-surface (Claude Code / Codex / Claude Desktop) with a live path preview sourced from `client_status()` before any files are touched. The uninstaller is a per-surface multi-select with a granular keep/delete checklist for CLI local state (venv/config/database/mcpb/servers), each destructive data category behind its own typed-`DELETE` gate and the shared-database warning; new `remove_claude_code()`/`remove_codex()` + `neo-localmcp remove-client` back it. A silent data-loss bug (PowerShell array-splat dropping every switch, so a confirmed database delete was preserved) was caught by live testing and fixed with a hashtable splat; a wizard-driving regression test now guards it. Full regression suite passes including the real install/uninstall subprocess integration tests.

## Remaining validation

- Run the targeted Claude Desktop extension/`uv.exe` tree smoke on a Windows x64 host where that tree is available; it was absent here. Re-run the Phase 14 commands specifically under Python 3.12 when that interpreter is available (current Windows host had only supported Python 3.14.5).

- Run the targeted Claude Desktop `.mcpb` installation smoke manually on macOS/Windows.
- Local Ollama auto-start and model warm-up were verified from a genuine connection-refused state; readiness recovered in under 18 seconds without automatic model eviction.
- Validate Claude Desktop installation from the generated `.mcpb` package.
- **Reconfirmed live 2026-07-03** (see below): `ollama_client.py`'s `start_service()` does not reliably inherit a custom `OLLAMA_MODELS` env var when spawned under some process ancestries (e.g. under Claude Desktop's extension host), silently falling back to the near-empty default models path instead of erroring. Not yet fixed.

## Known limitations

- Symbol extraction remains regex-based rather than a full Tree-sitter structural index.
- Token counts are estimated from returned characters until client usage telemetry is available.
- Compatibility alias `context_prepare` remains exposed for one release.
- Linux setup lifecycle and CI support are deferred beyond 1.0.10.
- Section-summary caching is keyed on source-file content hash only, not on the neo-localmcp code version -- a cache entry generated by a buggy older version is not automatically invalidated when the bug is fixed; only a source-file content change (or manual cache clear) will regenerate it.
- **Ollama model swap does not invalidate existing summaries, by deliberate decision (issue #8, 2026-07-04).** Swapping `fast_model`/`summary_model` via `set-ollama`/config never errors or corrupts memory: ranking is always live (never cached) and `retrieval_boost` has no model column. Summaries *are* tagged with the producing model (`files.summary_model`, `section_summaries.model`), but there is no "model changed -> invalidate/regenerate" trigger -- after a swap the old summaries keep being served as-is, tagged with the previous model, until the source file's content hash changes (which re-summarizes) or the cache is manually cleared. Verified in code: `tools.py` `_summarize_section` only compares the cached model when a `model` arg is explicitly passed, and the `summarize_file` tool/CLI defaults it to `None`, so the normal path short-circuits the comparison and serves the old-model summary; `set_ollama` writes config only and touches no summary row. This is intended, not a gap to "fix": summaries are advisory enrichment (see `docs/ARCHITECTURE.md` Safety model), never authoritative, so an older model's description of an unchanged file remains accurate. Chose status quo (option 1) over adding invalidate-on-swap machinery to `set-ollama`, per the repo's minimalism convention and the absence of any concrete correctness problem with stale-but-valid summaries. Documented in README.md's model-upgradability bullet and CLAUDE.md's Known gaps so a future contributor doesn't "fix" it without knowing it's deliberate. **This design call should be confirmed by the repo owner in PR review, not treated as silently final.**
- **Semantic embeddings (12b) backfill lazily, by design.** Setting `ollama.embed_model` on an already-indexed repo does not retroactively embed every file — `index_repo` only embeds *changed* paths. Embeddings populate as files change, or immediately via a forced reindex (`neo-localmcp reindex`). Until a repo has embeddings, the semantic re-rank is a strict no-op (byte-identical to pre-12b), so this is a "not yet enhanced" state, never a broken one. The same laziness means a repo whose Ollama was down during indexing simply has no embeddings that pass — they backfill on the next successful index.
- `start_service()` may silently use the wrong Ollama models directory depending on process ancestry (see Remaining validation above).
- **`tests/installer/test_verification.py` makes live Ollama network calls (found 2026-07-05, not yet fixed).** `_base_kwargs()` never stubs `ollama_status_fn`, so ~11-14 "unit" tests call the real `ollama_client.status()` against whatever host `~/.neo-localmcp/config.yaml` configures on the running machine -- non-hermetic, and ~3s slower per test (matching `connect_timeout_seconds=3`) than a stubbed call. Discovered while investigating CI Phase 3; spun off as a separate follow-up rather than fixed inline.
- **Fixed 2026-07-03**: `utils.rg_search`'s pure-Python fallback (used whenever `rg` is not on PATH -- e.g. GitHub's `macos-latest` CI runner has no ripgrep preinstalled, unlike `windows-latest`) did a literal substring match of the *entire* regex-syntax query string tools.py always sends it (`"(?:term1|term2)"`), which never occurs verbatim in real source text -- so batched search silently returned zero results on any host without `rg`, for single- or multi-term queries alike. Surfaced by CI (not local dev, since `rg` was present there) after the #2 fix above added a test relying purely on batched-search matching. Now compiles and searches the query as an actual regex. Covered by `tests/test_utils.py::test_rg_search_fallback_interprets_the_query_as_a_regex`.
- Retrieval-boost memory: "can this be counted on to improve retrieval over time?" is now a qualified yes, not the previous no. It provably works and survives upgrades (see Verified), and it can never hurt correctness -- it's capped far below structural evidence and never penalizes silence. But its impact is modest by design: it only nudges once the *same task string* recurs >= 3 times in one repo, so it helps repeated workflows in a repo, not first-time or one-off tasks, and its real-world benefit has still only been exercised in one controlled audit session, not measured across sustained multi-session usage. It is a safe, bounded nudge, not a broadly-learning ranker.
- **Fixed 2026-07-03**: `config.py`'s default `repo.exclude_dirs` (`.venv`, `venv`, etc.) did exact directory-name matching only, not prefix/glob matching, so a differently-named local venv or vendor directory (e.g. a disposable `.venv-phase14` test venv, or this project's own installer convention `.venv-nlm-v<version>`) got indexed and its vendored dependency code could outrank real repository source. `utils.scan_repo_files` now matches `exclude_dirs` entries as `fnmatch` glob patterns (plain names still match exactly); defaults broadened to `.venv*`/`venv*`. Covered by `tests/test_utils.py`.
- **Fixed 2026-07-03**: when a candidate file had multiple independent hint locations for different matched terms, `tools.py`'s excerpt-centering logic took whichever hint sorted first by line number rather than the hint tied to the term that actually matched the query (reproduced live: a query for `num_predict` matched `ollama_client.py` at line 308, but the shown excerpt centered on an unrelated earlier line 28). `_add_candidate` now tracks a `line_hint_weights` map (line number -> strongest evidence weight) and centering picks the highest-weight hint (ties break to the earliest line for determinism), falling back to the old first-hint behavior only when no weighted hint exists. Retrieval-boost's "followed" scoring inherited the same bug (`repo_memory.record_retrieval_feedback` only checked overlap against the one chosen window); fallback-centered `sections_for_memory` entries now also carry the file's other listed hint lines (`hint_lines`), and a `file_excerpts` pull overlapping any of them counts as followed, not just the primary window. Covered by `tests/test_context.py::test_excerpt_centers_on_highest_weight_hint_not_first_by_line` and `tests/test_retrieval_memory.py::test_follow_up_pull_on_a_secondary_hint_still_counts_as_followed`.
- **Fixed 2026-07-03**: a genuinely fresh clone could not follow the README's own `python3 setup.py install` instructions on a bare system Python. `setup_cli.py` unconditionally imports `neo_localmcp.installer`, which imports `installer/processes.py`'s top-level `import psutil` -- so the *host* interpreter invoking `setup.py` (not the managed runtime it builds) needed `psutil` importable before anything else ran, with no bootstrap step and no README mention. This had been masked because CI's `pip install -e ".[test]"` step always installs the full dependency set (including `psutil`) before any lifecycle test runs. Reproduced live with a genuinely bare venv (`ModuleNotFoundError: No module named 'psutil'` from `setup.py install --dry-run`) and confirmed the fix resolves it (same bare venv, `psutil` installed first, dry-run completes cleanly). Fixed by documenting the prerequisite in `README.md`'s Install section (`python3 -m pip install "psutil>=5.9,<8"` before running `setup.py`) rather than changing import structure, since `psutil` is needed almost immediately regardless.

## Next milestone

Finish 1.1.1 and ship it: #65 (`--live-agent-comparison`) is the one remaining piece of designed-but-not-built scope (see priority order in `PROJECT_NOTES.md` 2026-07-06). Beyond that, the cross-platform acceptance and token benchmark — package on Windows/macOS, run representative Python and C#/XAML tasks, compare discovery and total tokens, and tune excerpt selection from measured misses — remains the longer-term goal, but has no version or plan assigned yet.
