---
description: Show repository index and Neo LocalMCP status
argument-hint: "[repo-path]"
---

Call the `repo_status` MCP tool with an explicit `repo_root` and report the repository index state. Use `ollama_status` only when the user specifically asks for model readiness.
